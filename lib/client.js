window.__ModuleLoader__.load({
  id: "dsh-todo-float-ball",
  factory: function (require) {
    var module = { exports: {} };
    var exports = module.exports;
    // skin-catalog.js — shared 128-skin catalog for dsh-todo-float-ball /
    // dsh-skill-browser / dsh-font-enhancer. Generated by make_skin_catalog.py.
    // Proposal numbering matches the approved design sheets (01-128).
    (function () {
      if (window.__DSH_BALL_SKINS) return;
      function hs(h,s,l,a){return a==null?("hsl("+h+","+s+"%,"+l+"%)"):("hsla("+h+","+s+"%,"+l+"%,"+a+")");}
      var FORMULA = {
        deep:[["朱砂","VERMILION",4],["酡颜","FLUSHED",13],["琥珀","AMBER",32],["缃色","PALE GOLD",44],["藤黄","GAMBOGE",52],["松花","PINE POLLEN",62],["葱倩","SCALLION",78],["艾绿","MUGWORT",96],["嫩柳","WILLOW",112],["竹青","BAMBOO",126],["石绿","MINERAL GREEN",138],["翡翠","JADEITE",152],["天水碧","SKY WATER",164],["铜绿","VERDIGRIS",172],["松石","TURQUOISE",180],["孔雀","PEACOCK",188],["晴山","CLEAR HILL",194],["天青","AZURE",200],["湖水","LAKE BLUE",207],["霁蓝","AFTER RAIN",214],["群青","ULTRAMARINE",221],["靛蓝","INDIGO",229],["绀青","NAVY",236],["藏蓝","TIBETAN",243],["窃蓝","FLEDGLING BLUE",250],["青莲","LOTUS VIOLET",258],["紫藤","WISTERIA",265],["紫棠","VIOLET WOOD",272],["雪青","SNOW LILAC",283],["丁香","LILAC",290],["堇色","VIOLET",297],["藕荷","LOTUS PINK",305],["烟霞","MISTY GLOW",315],["十样锦","BROCADE",326],["海棠","BEGONIA",334],["银红","SILVER RED",341],["桃夭","PEACH BLOSSOM",349],["绛纱","CRIMSON GAUZE",356]],
        muted:[["栗","CHESTNUT",16],["驼","CAMEL",26],["灰陶","POTTERY",34],["枯茶","DRIED TEA",42],["秋香","AUTUMN",58],["苔径","MOSS PATH",88],["豆绿","BEAN GREEN",98],["青梅","GREEN PLUM",116],["苍山","PALE MOUNTAIN",150],["青矾","VITRIOL",194],["沧浪","PALE WAVE",205],["雾蓝","FOG BLUE",212],["青灰","BLUISH GRAY",224],["黛蓝","HAZY BLUE",232],["黛灰","INK GRAY",252],["紫灰","VIOLET GRAY",276],["烟紫","SMOKY VIOLET",288],["藕灰","LOTUS GRAY",308],["绛紫","CRIMSON PLUM",326],["檀","SANDLEWOOD",359]],
        soft:[["曙光","FIRST LIGHT",8],["橄榄","OLIVE",66],["松间","PINEWOOD",140],["青栀","GARDENIA",168],["湖烟","LAKE MIST",190],["碧涛","BLUE WAVE",202],["竹月","BAMBOO MOON",216],["星蓝","STAR BLUE",226],["青冥","AZURE DEPTH",244],["紫冥","DUSK VIOLET",262],["夜堇","NIGHT VIOLET",284],["芋紫","TARO",300],["霞粉","GLOW PINK",338],["树莓","RASPBERRY",344],["樱贝","CHERRY SHELL",352]],
        neutral:[["银鼠","SILVER RAT",205],["玄青","INK CYAN",160],["玄墨","JET BLACK",275],["乌羽","RAVEN",8],["烟灰","SMOKE",32],["灰鹄","SWAN",140],["玄天","DARK SKY",222],["黛烟","INK SMOKE",288],["铁灰","IRON",210],["磨石","WHETSTONE",95]],
        light:[["月白","MOON WHITE",210],["卵白","EGG WHITE",88],["牙色","IVORY",46],["茶白","TEA WHITE",28],["米浆","RICE MILK",52],["亚麻","LINEN",32],["藕丝","LOTUS THREAD",330],["绫粉","SILK PINK",350],["青白","PALE CELADON",160],["天际","SKYLINE",205],["紫丁","PALE LILAC",285],["雾粉","MIST PINK",345],["薄荷","MINT",150],["绢黄","PALE SILK",48],["灰绿","SAGE",110],["雪浪","SNOW WAVE",195],["燕羽","SWALLOW",225]],
      };
      function varsFor(kind,hh){
        var v={};
        if(kind==="deep"){v={bgA:hs(hh,42,17),bgB:hs(hh,48,7),edge:hs(hh,60,85,".2"),spec:"rgba(255,255,255,.16)",stI:hs(hh,14,54),stP:hs(hh,40,62),stA:hs(hh,88,68),stD:hs(hh,50,42),lbc:hs(hh,35,94),trk:hs(hh,50,80,".13"),pbgA:hs(hh,24,12),pbgB:hs(hh,28,7),pbd:hs(hh,45,80,".14"),ptx:hs(hh,28,92),itm:hs(hh,16,66),itD:hs(hh,18,52),cctx:hs(hh,16,66),chipBg:hs(hh,30,60,".07"),hintC:hs(hh,12,45),hair1:hs(hh,88,68),hair2:hs(hh,50,42)};}
        else if(kind==="muted"){v={bgA:hs(hh,19,15),bgB:hs(hh,20,6),edge:hs(hh,30,78,".17"),spec:"rgba(255,255,255,.1)",stI:hs(hh,10,50),stP:hs(hh,22,56),stA:hs(hh,42,62),stD:hs(hh,28,40),lbc:hs(hh,18,90),trk:hs(hh,30,75,".12"),pbgA:hs(hh,14,11),pbgB:hs(hh,16,6),pbd:hs(hh,25,75,".12"),ptx:hs(hh,14,88),itm:hs(hh,10,62),itD:hs(hh,12,48),cctx:hs(hh,10,62),chipBg:hs(hh,18,55,".06"),hintC:hs(hh,8,42),hair1:hs(hh,42,62),hair2:hs(hh,28,40)};}
        else if(kind==="soft"){v={bgA:hs(hh,28,21),bgB:hs(hh,30,10),edge:hs(hh,45,85,".22"),spec:"rgba(255,255,255,.2)",stI:hs(hh,16,58),stP:hs(hh,36,66),stA:hs(hh,80,70),stD:hs(hh,44,46),lbc:hs(hh,30,95),trk:hs(hh,45,85,".14"),pbgA:hs(hh,20,15),pbgB:hs(hh,24,9),pbd:hs(hh,40,85,".15"),ptx:hs(hh,24,93),itm:hs(hh,14,70),itD:hs(hh,16,56),cctx:hs(hh,14,70),chipBg:hs(hh,26,65,".08"),hintC:hs(hh,10,50),hair1:hs(hh,80,70),hair2:hs(hh,44,46)};}
        else if(kind==="neutral"){v={bgA:hs(hh,7,18),bgB:hs(hh,8,7),edge:"rgba(255,255,255,.14)",spec:"rgba(255,255,255,.12)",stI:hs(hh,10,52),stP:hs(hh,28,60),stA:hs(hh,62,64),stD:hs(hh,38,42),lbc:hs(hh,10,93),trk:hs(hh,20,80,".12"),pbgA:hs(hh,6,12),pbgB:hs(hh,7,7),pbd:"rgba(255,255,255,.09)",ptx:hs(hh,8,90),itm:hs(hh,8,64),itD:hs(hh,10,50),cctx:hs(hh,8,64),chipBg:"rgba(255,255,255,.05)",hintC:hs(hh,6,42),hair1:hs(hh,62,64),hair2:hs(hh,38,42)};}
        else{v={bgA:hs(hh,36,96),bgB:hs(hh,26,84),edge:hs(hh,30,22,".26"),spec:"rgba(255,255,255,.85)",hi:"rgba(255,255,255,.7)",stI:hs(hh,10,60),stP:hs(hh,26,44),stA:hs(hh,40,30),stD:hs(hh,28,40),lbc:hs(hh,25,20),trk:hs(hh,25,20,".14"),pbgA:hs(hh,45,97),pbgB:hs(hh,32,90),pbd:hs(hh,30,25,".15"),ptx:hs(hh,25,20),itm:hs(hh,12,40),itD:hs(hh,14,52),cctx:hs(hh,12,42),chipBg:hs(hh,25,25,".06"),hintC:hs(hh,10,55),hair1:hs(hh,40,30),hair2:hs(hh,28,40)};}
        return v;
      }
      var EX={};
      var EX_ORDER=[];
      EX["01"]={no:"01",name:"曜石极光",en:"OBSIDIAN AURORA",group:"招牌特效",anim:"flashy",vars:{bgA:"#273049",bgB:"#090c15",edge:"rgba(255,255,255,.16)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#f2f7ff",trk:"rgba(255,255,255,.1)",stI:"#8fa0b8",stP:"#6fb6ff",stA:"#ffb454",stD:"#34e39a",pbgA:"#171c2b",pbgB:"#0c0f18",pbd:"rgba(255,255,255,.09)",ptx:"#dfe7f3",itm:"#b6c6dd",itD:"#7d9c8d",cctx:"#b6c6dd",chipBg:"rgba(255,255,255,.06)",hintC:"#5f6b80",hair1:"#34e39a",hair2:"#7c6bff"},rim:"%RIM%{position:absolute;inset:-3px;border-radius:inherit;pointer-events:none;background:conic-gradient(from 0deg,transparent 0 68%,var(--stA) 86%,#fff 93%,transparent 100%);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 3px),#000 calc(100% - 2px));mask:radial-gradient(farthest-side,transparent calc(100% - 3px),#000 calc(100% - 2px));filter:drop-shadow(0 0 6px var(--stA));opacity:.95;animation:dshSkinSpin 2.6s linear infinite}",band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("01");
      EX["02"]={no:"02",name:"赛博机能",en:"CYBER CIRCUIT",group:"招牌特效",anim:"flashy",vars:{bgA:"#0d1b2e",bgB:"#04080f",edge:"rgba(0,229,255,.4)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#c9e7f5",trk:"rgba(255,255,255,.1)",stI:"#4f7b95",stP:"#00e5ff",stA:"#ff2d95",stD:"#00ff9d",pbgA:"#0a1622",pbgB:"#050b12",pbd:"rgba(0,229,255,.24)",ptx:"#c9e7f5",itm:"#9fc3d8",itD:"#5f9a85",cctx:"#9fc3d8",chipBg:"rgba(0,229,255,.06)",hintC:"#4f7b95",hair1:"#00e5ff",hair2:"#ff2d95"},rim:"%RIM%{position:absolute;inset:-2px;border-radius:inherit;pointer-events:none;border:1px dashed var(--stP);animation:dshSkinSpin 6s linear infinite}",band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:repeating-linear-gradient(0deg,rgba(0,229,255,.07) 0 1px,transparent 1px 3px)}"};
      EX_ORDER.push("02");
      EX["03"]={no:"03",name:"等离子核心",en:"PLASMA CORE",group:"招牌特效",anim:"flashy",vars:{bgA:"#2a1210",bgB:"#0d0403",edge:"rgba(255,140,60,.4)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#f5e3d5",trk:"rgba(255,255,255,.1)",stI:"#8a5a4a",stP:"#ffa23d",stA:"#ff5c2b",stD:"#ffd23d",pbgA:"#1c0e0a",pbgB:"#0c0504",pbd:"rgba(255,140,60,.22)",ptx:"#f5e3d5",itm:"#c8a795",itD:"#96826e",cctx:"#c8a795",chipBg:"rgba(255,140,60,.07)",hintC:"#8a5a4a",hair1:"#ff9a3d",hair2:"#ff3d81"},rim:"%RIM%{position:absolute;inset:-1px;border-radius:inherit;pointer-events:none;background:conic-gradient(from 40deg,rgba(255,154,61,0),#ff9a3d,#ff3d81,#3d9aff,rgba(61,154,255,0) 82%);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 3px),#000 calc(100% - 1px));mask:radial-gradient(farthest-side,transparent calc(100% - 3px),#000 calc(100% - 1px));filter:blur(2.5px);animation:dshSkinSpin 3s linear infinite}"};
      EX_ORDER.push("03");
      EX["04"]={no:"04",name:"全息幻彩",en:"HOLOGRAPHIC",group:"招牌特效",anim:"flashy",vars:{bgA:"#b8a6f5",bgB:"#6ec6ee",edge:"rgba(255,255,255,.65)",spec:"rgba(255,255,255,.5)",hi:"rgba(255,255,255,.6)",lbc:"#241d4f",trk:"rgba(36,29,79,.2)",stI:"#7c86a8",stP:"#6a5df0",stA:"#e0559f",stD:"#0fae84",pbgA:"#f2f0ff",pbgB:"#e2e8ff",pbd:"rgba(106,93,240,.28)",ptx:"#2a2547",itm:"#4a5470",itD:"#57806c",cctx:"#4a5470",chipBg:"rgba(106,93,240,.08)",hintC:"#7a7fa0",hair1:"#e0559f",hair2:"#6a5df0"},rim:"%RIM%{position:absolute;inset:-3px;border-radius:inherit;pointer-events:none;background:conic-gradient(from 0deg,#ff9ad5,#ffd1a1,#a1ffd1,#9ad5ff,#b89aff,#ff9ad5);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 2px),#000 calc(100% - 1px));mask:radial-gradient(farthest-side,transparent calc(100% - 2px),#000 calc(100% - 1px));filter:blur(3px);opacity:.8;animation:dshSkinSpin 9s linear infinite}",band:"%BAND%{position:absolute;inset:3px 4px 55% 4px;border-radius:inherit;pointer-events:none;background:linear-gradient(rgba(255,255,255,.7),rgba(255,255,255,.05));filter:blur(.5px)}"};
      EX_ORDER.push("04");
      EX["05"]={no:"05",name:"量子矩阵",en:"QUANTUM MATRIX",group:"招牌特效",anim:"flashy",vars:{bgA:"#0c1f12",bgB:"#020804",edge:"rgba(0,255,136,.4)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#c9f5d9",trk:"rgba(255,255,255,.1)",stI:"#4f8a63",stP:"#27e584",stA:"#a3ff12",stD:"#00ff9d",pbgA:"#05130a",pbgB:"#020a05",pbd:"rgba(0,255,136,.25)",ptx:"#c9f5d9",itm:"#8fc7a5",itD:"#5f9a78",cctx:"#8fc7a5",chipBg:"rgba(0,255,136,.06)",hintC:"#4f8a63",hair1:"#27e584",hair2:"#a3ff12"},rim:"%RIM%{position:absolute;inset:-2px;border-radius:inherit;pointer-events:none;border:1px dashed rgba(0,255,136,.5);animation:dshSkinSpinR 5s linear infinite}",band:"%BAND%{position:absolute;inset:4px;border-radius:inherit;pointer-events:none;background-image:radial-gradient(rgba(0,255,136,.55) 1px,transparent 1.4px);background-size:7px 7px;-webkit-mask:radial-gradient(circle,#000 0 62%,transparent 74%);mask:radial-gradient(circle,#000 0 62%,transparent 74%);opacity:.5}"};
      EX_ORDER.push("05");
      EX["06"]={no:"06",name:"星环",en:"STAR HALO",group:"招牌特效",anim:"flashy",vars:{bgA:"#131a33",bgB:"#05070f",edge:"rgba(186,160,255,.35)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#e2dcff",trk:"rgba(255,255,255,.1)",stI:"#7d86a8",stP:"#8f9fff",stA:"#ffc46e",stD:"#b28bff",pbgA:"#101527",pbgB:"#070a14",pbd:"rgba(150,130,255,.22)",ptx:"#e2dcff",itm:"#a99cd0",itD:"#8678b0",cctx:"#a99cd0",chipBg:"rgba(150,130,255,.07)",hintC:"#7d86a8",hair1:"#8f9fff",hair2:"#b28bff"},rim:"%RIM%{position:absolute;inset:-7px -15px;border-radius:50%;pointer-events:none;border:1.5px solid rgba(186,160,255,.65);transform:rotate(-16deg) scaleY(.42);box-shadow:0 0 10px rgba(150,120,255,.45),inset 0 0 6px rgba(150,120,255,.3)}",band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background-image:radial-gradient(rgba(255,255,255,.8) 1px,transparent 1.3px),radial-gradient(rgba(186,160,255,.7) .8px,transparent 1.1px);background-size:21px 19px,13px 17px;background-position:4px 6px,9px 12px;opacity:.5;-webkit-mask:radial-gradient(circle,#000 0 55%,transparent 72%);mask:radial-gradient(circle,#000 0 55%,transparent 72%)}"};
      EX_ORDER.push("06");
      EX["07"]={no:"07",name:"熔岩核心",en:"MAGMA CORE",group:"招牌特效",anim:"flashy",vars:{bgA:"#1d0f0a",bgB:"#0a0403",edge:"rgba(255,110,40,.4)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#f5ded0",trk:"rgba(255,255,255,.1)",stI:"#7d5648",stP:"#ff8a3d",stA:"#ff4d1f",stD:"#ffbe3d",pbgA:"#170d09",pbgB:"#0b0504",pbd:"rgba(255,110,40,.22)",ptx:"#f5ded0",itm:"#c8a48e",itD:"#96826e",cctx:"#c8a48e",chipBg:"rgba(255,110,40,.07)",hintC:"#7d5648",hair1:"#ff6b2b",hair2:"#ffbe3d"},band:"%BAND%{position:absolute;inset:2px;border-radius:inherit;pointer-events:none;background:repeating-conic-gradient(from 12deg,transparent 0 26deg,rgba(255,110,40,.6) 26deg 29deg);-webkit-mask:radial-gradient(circle,transparent 0 46%,#000 60% 78%,transparent 88%);mask:radial-gradient(circle,transparent 0 46%,#000 60% 78%,transparent 88%);opacity:.75}"};
      EX_ORDER.push("07");
      EX["08"]={no:"08",name:"极地冰川",en:"GLACIER",group:"招牌特效",anim:"flashy",vars:{bgA:"#dff0fa",bgB:"#9fc8e8",edge:"rgba(255,255,255,.78)",spec:"rgba(255,255,255,.75)",hi:"rgba(255,255,255,.7)",lbc:"#123247",trk:"rgba(20,60,90,.18)",stI:"#6f8ba5",stP:"#2f9dd8",stA:"#f06a5d",stD:"#0fa878",pbgA:"#eef7fd",pbgB:"#d8ecf8",pbd:"rgba(60,120,170,.3)",ptx:"#14344a",itm:"#3f5f7a",itD:"#4a7a63",cctx:"#3f5f7a",chipBg:"rgba(30,80,120,.07)",hintC:"#5b7d96",hair1:"#2f9dd8",hair2:"#0fa878"},band:"%BAND%{position:absolute;inset:2px;border-radius:inherit;pointer-events:none;background-image:radial-gradient(rgba(255,255,255,.95) 1px,transparent 1.5px);background-size:15px 13px;background-position:3px 4px;opacity:.8;-webkit-mask:radial-gradient(circle,#000 0 58%,transparent 75%);mask:radial-gradient(circle,#000 0 58%,transparent 75%)}"};
      EX_ORDER.push("08");
      EX["09"]={no:"09",name:"霓虹脉搏",en:"NEON PULSE",group:"招牌特效",anim:"flashy",vars:{bgA:"#10101f",bgB:"#05050c",edge:"rgba(255,45,149,.5)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#f0d9ec",trk:"rgba(255,255,255,.1)",stI:"#5a5a7d",stP:"#00c8ff",stA:"#ff2d95",stD:"#00ff9d",pbgA:"#0d0d1a",pbgB:"#060609",pbd:"rgba(255,45,149,.3)",ptx:"#f0d9ec",itm:"#b893c9",itD:"#7d9c8d",cctx:"#b893c9",chipBg:"rgba(255,45,149,.07)",hintC:"#8b8bb0",hair1:"#ff2d95",hair2:"#00c8ff"},rim:"%RIM%{position:absolute;inset:-7px;border-radius:inherit;pointer-events:none;border:1px solid rgba(0,200,255,.3)}",extra:"%B%{box-shadow:inset 0 1px 0 rgba(255,255,255,.14),0 5px 14px rgba(0,0,0,.5),0 0 12px var(--st),0 0 0 1px var(--st) inset!important}"};
      EX_ORDER.push("09");
      EX["10"]={no:"10",name:"机械核心",en:"MECH CORE",group:"招牌特效",anim:"flashy",vars:{bgA:"#262b33",bgB:"#0e1116",edge:"rgba(255,176,32,.45)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#dde3ec",trk:"rgba(255,255,255,.1)",stI:"#6f7a88",stP:"#58a6ff",stA:"#ffb020",stD:"#3fd68a",pbgA:"#171b22",pbgB:"#0c0f14",pbd:"rgba(255,176,32,.22)",ptx:"#dde3ec",itm:"#9aa5b3",itD:"#7d9c8d",cctx:"#9aa5b3",chipBg:"rgba(255,176,32,.06)",hintC:"#6f7a88",hair1:"#ffb020",hair2:"#58a6ff"},rim:"%RIM%{position:absolute;inset:-4px;border-radius:inherit;pointer-events:none;background:repeating-conic-gradient(from 0deg,rgba(255,176,32,.55) 0 7deg,transparent 7deg 24deg);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 3.5px),#000 calc(100% - 2px));mask:radial-gradient(farthest-side,transparent calc(100% - 3.5px),#000 calc(100% - 2px));animation:dshSkinSpinR 11s linear infinite}",band:"%BAND%{position:absolute;inset:-1px;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 4%,rgba(255,255,255,.7) 1.2px,transparent 1.8px),radial-gradient(circle at 50% 96%,rgba(255,255,255,.7) 1.2px,transparent 1.8px),radial-gradient(circle at 4% 50%,rgba(255,255,255,.7) 1.2px,transparent 1.8px),radial-gradient(circle at 96% 50%,rgba(255,255,255,.7) 1.2px,transparent 1.8px);opacity:.6}"};
      EX_ORDER.push("10");
      EX["11"]={no:"11",name:"暗物质",en:"DARK MATTER",group:"招牌特效",anim:"flashy",vars:{bgA:"#100a1f",bgB:"#030109",edge:"rgba(160,110,255,.4)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#e4d9ff",trk:"rgba(255,255,255,.1)",stI:"#5f5480",stP:"#9d6bff",stA:"#d05cff",stD:"#7af0ff",pbgA:"#120c20",pbgB:"#070310",pbd:"rgba(160,110,255,.25)",ptx:"#e4d9ff",itm:"#a99cc9",itD:"#7d9c8d",cctx:"#a99cc9",chipBg:"rgba(160,110,255,.07)",hintC:"#9082b3",hair1:"#d05cff",hair2:"#7af0ff"},rim:"%RIM%{position:absolute;inset:-2px;border-radius:inherit;pointer-events:none;background:conic-gradient(from 90deg,transparent 0 30%,rgba(208,92,255,.9) 52%,rgba(122,240,255,.7) 68%,transparent 88%);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 4px),#000 calc(100% - 1px));mask:radial-gradient(farthest-side,transparent calc(100% - 4px),#000 calc(100% - 1px));filter:blur(1.5px);animation:dshSkinSpin 2.2s linear infinite}",band:"%BAND%{position:absolute;inset:9px;border-radius:inherit;pointer-events:none;background:radial-gradient(circle,#000 0 55%,rgba(122,240,255,.25) 78%,transparent)}"};
      EX_ORDER.push("11");
      EX["12"]={no:"12",name:"粒子星云",en:"PARTICLE NEBULA",group:"招牌特效",anim:"flashy",vars:{bgA:"#1b1440",bgB:"#070516",edge:"rgba(200,140,255,.35)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#e8def5",trk:"rgba(255,255,255,.1)",stI:"#7d72a8",stP:"#7fb0ff",stA:"#ff8ad0",stD:"#8affd0",pbgA:"#150f30",pbgB:"#090617",pbd:"rgba(170,130,255,.22)",ptx:"#e8def5",itm:"#b0a3cc",itD:"#7d9c8d",cctx:"#b0a3cc",chipBg:"rgba(170,130,255,.07)",hintC:"#7d72a8",hair1:"#ff8ad0",hair2:"#7fb0ff"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background-image:radial-gradient(rgba(255,138,208,.9) 1.1px,transparent 1.6px),radial-gradient(rgba(127,176,255,.85) .9px,transparent 1.4px),radial-gradient(rgba(255,255,255,.9) .8px,transparent 1.2px);background-size:19px 17px,14px 21px,23px 13px;background-position:5px 7px,11px 14px,2px 16px;opacity:.75;-webkit-mask:radial-gradient(circle,#000 0 56%,transparent 74%);mask:radial-gradient(circle,#000 0 56%,transparent 74%)}"};
      EX_ORDER.push("12");
      EX["13"]={no:"13",name:"流体渐变",en:"FLUID GRADIENT",group:"招牌特效",anim:"flashy",vars:{bgA:"#7c5cf0",bgB:"#2a1a5e",edge:"rgba(255,255,255,.32)",spec:"rgba(255,255,255,.4)",hi:"rgba(255,255,255,.12)",lbc:"#f2edff",trk:"rgba(255,255,255,.1)",stI:"#8f86b8",stP:"#4dd8ff",stA:"#ff7ab0",stD:"#4dffc0",pbgA:"#151033",pbgB:"#0a0720",pbd:"rgba(180,150,255,.25)",ptx:"#ece4ff",itm:"#b3a6d9",itD:"#7d9c8d",cctx:"#b3a6d9",chipBg:"rgba(180,150,255,.08)",hintC:"#8f86b8",hair1:"#4dd8ff",hair2:"#ff7ab0"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 28% 68%,rgba(255,122,176,.55),transparent 55%),radial-gradient(circle at 74% 26%,rgba(77,216,255,.5),transparent 52%)}"};
      EX_ORDER.push("13");
      EX["14"]={no:"14",name:"黄金时代",en:"GOLDEN AGE",group:"招牌特效",anim:"flashy",vars:{bgA:"#241d0d",bgB:"#0c0904",edge:"rgba(255,215,0,.45)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#ffefc4",trk:"rgba(255,255,255,.1)",stI:"#8a7a4f",stP:"#e6c34d",stA:"#ffb020",stD:"#58d68d",pbgA:"#16120a",pbgB:"#0a0805",pbd:"rgba(255,215,0,.28)",ptx:"#f2e6c8",itm:"#c2b48c",itD:"#7d9c8d",cctx:"#c2b48c",chipBg:"rgba(255,215,0,.06)",hintC:"#8a7a4f",hair1:"#ffd700",hair2:"#e6c34d"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:linear-gradient(115deg,transparent 30%,rgba(255,236,170,.75) 47%,rgba(255,255,255,.9) 50%,rgba(255,236,170,.75) 53%,transparent 70%);background-size:260% 100%;background-position:118% 0;-webkit-mask:radial-gradient(circle,#000 0 62%,transparent 78%);mask:radial-gradient(circle,#000 0 62%,transparent 78%)}"};
      EX_ORDER.push("14");
      EX["15"]={no:"15",name:"蒸汽波",en:"VAPORWAVE",group:"招牌特效",anim:"flashy",vars:{bgA:"#2b1140",bgB:"#0d0518",edge:"rgba(255,94,200,.5)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#f5dcf0",trk:"rgba(255,255,255,.1)",stI:"#7d5a95",stP:"#22d3ee",stA:"#ff5ec8",stD:"#3dffc8",pbgA:"#170b26",pbgB:"#0b0513",pbd:"rgba(255,94,200,.3)",ptx:"#f5dcf0",itm:"#c39cc9",itD:"#7d9c8d",cctx:"#c39cc9",chipBg:"rgba(255,94,200,.07)",hintC:"#7d5a95",hair1:"#ff5ec8",hair2:"#22d3ee"},band:"%BAND%{position:absolute;inset:2px 3px 52% 3px;border-radius:inherit;pointer-events:none;background:linear-gradient(rgba(255,150,220,.4),rgba(120,240,255,.08));filter:blur(.5px)}",extra:"%B%{box-shadow:inset 0 1px 0 rgba(255,255,255,.14),0 5px 14px rgba(0,0,0,.5),-4px -4px 16px rgba(255,94,200,.45),4px 4px 16px rgba(34,211,238,.45)!important}"};
      EX_ORDER.push("15");
      EX["16"]={no:"16",name:"战术HUD",en:"TACTICAL HUD",group:"招牌特效",anim:"flashy",vars:{bgA:"#0f1a12",bgB:"#050b07",edge:"rgba(74,222,128,.45)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#d2f5dc",trk:"rgba(255,255,255,.1)",stI:"#5a7d66",stP:"#4ade80",stA:"#a3e635",stD:"#34e39a",pbgA:"#0c1710",pbgB:"#060d08",pbd:"rgba(74,222,128,.28)",ptx:"#d2f5dc",itm:"#8fb89c",itD:"#5f9a78",cctx:"#8fb89c",chipBg:"rgba(74,222,128,.06)",hintC:"#5a7d66",hair1:"#4ade80",hair2:"#a3e635"},rim:"%RIM%{position:absolute;inset:-5px;border-radius:inherit;pointer-events:none;background:repeating-conic-gradient(from 0deg,rgba(74,222,128,.5) 0 2deg,transparent 2deg 15deg);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 2.5px),#000 calc(100% - 1px));mask:radial-gradient(farthest-side,transparent calc(100% - 2.5px),#000 calc(100% - 1px));animation:dshSkinSpin 14s linear infinite}",band:"%BAND%{position:absolute;inset:7px;border-radius:inherit;pointer-events:none;background:linear-gradient(rgba(74,222,128,.55),rgba(74,222,128,.55)) 50% 0/1px 7px no-repeat,linear-gradient(rgba(74,222,128,.55),rgba(74,222,128,.55)) 50% 100%/1px 7px no-repeat,linear-gradient(rgba(74,222,128,.55),rgba(74,222,128,.55)) 0 50%/7px 1px no-repeat,linear-gradient(rgba(74,222,128,.55),rgba(74,222,128,.55)) 100% 50%/7px 1px no-repeat;opacity:.7}"};
      EX_ORDER.push("16");
      EX["17"]={no:"17",name:"方块像素",en:"PIXEL QUEST",group:"招牌特效",anim:"flashy",vars:{bgA:"#3b4268",bgB:"#24283d",edge:"#0f1120",spec:"rgba(255,255,255,.22)",hi:"rgba(255,255,255,.1)",lbc:"#dfe4ff",trk:"rgba(16,18,31,.8)",stI:"#8a90b8",stP:"#41a6f6",stA:"#ffcd75",stD:"#38b764",pbgA:"#2a2f4a",pbgB:"#1a1d33",pbd:"rgba(15,17,32,.9)",ptx:"#dfe4ff",itm:"#9aa3cc",itD:"#7d9c8d",cctx:"#9aa3cc",chipBg:"rgba(255,255,255,.06)",hintC:"#9aa3c2",hair1:"#ffcd75",hair2:"#41a6f6"},extra:"%B%{border-radius:10px!important;box-shadow:inset 0 0 0 1px rgba(255,255,255,.1),5px 5px 0 rgba(0,0,0,.55),0 0 0 1px var(--edge)!important;font-family:Consolas,monospace!important}"};
      EX_ORDER.push("17");
      EX["18"]={no:"18",name:"磁悬浮轨道",en:"MAGLEV ORBIT",group:"招牌特效",anim:"flashy",vars:{bgA:"#141a24",bgB:"#070a10",edge:"rgba(90,209,230,.4)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#d8ecf2",trk:"rgba(255,255,255,.1)",stI:"#5f7a88",stP:"#5ad1e6",stA:"#ffb454",stD:"#34e39a",pbgA:"#121822",pbgB:"#080c12",pbd:"rgba(90,209,230,.25)",ptx:"#d8ecf2",itm:"#96b5c2",itD:"#7d9c8d",cctx:"#96b5c2",chipBg:"rgba(90,209,230,.06)",hintC:"#5f7a88",hair1:"#5ad1e6",hair2:"#34e39a"},rim:"%RIM%{position:absolute;inset:-7px -15px;border-radius:50%;pointer-events:none;border:1.3px solid rgba(90,209,230,.7);transform:rotateX(66deg) rotate(-18deg);box-shadow:0 0 9px rgba(90,209,230,.4)}",band:"%BAND%{position:absolute;inset:-7px -15px;border-radius:50%;pointer-events:none;border:1.3px solid rgba(52,227,154,.55);transform:rotateX(66deg) rotate(52deg);box-shadow:0 0 9px rgba(52,227,154,.35)}"};
      EX_ORDER.push("18");
      EX["19"]={no:"19",name:"太阳风暴",en:"SOLAR FLARE",group:"招牌特效",anim:"flashy",vars:{bgA:"#2a1a08",bgB:"#0f0803",edge:"rgba(255,220,120,.5)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#f8ecd2",trk:"rgba(255,255,255,.1)",stI:"#9a835a",stP:"#ffd23d",stA:"#ff9a1f",stD:"#ffe98a",pbgA:"#1c1206",pbgB:"#0d0803",pbd:"rgba(255,210,110,.28)",ptx:"#f8ecd2",itm:"#c9b491",itD:"#9a8a68",cctx:"#c9b491",chipBg:"rgba(255,210,110,.07)",hintC:"#9a835a",hair1:"#ffd23d",hair2:"#ff9a1f"},rim:"%RIM%{position:absolute;inset:-8px;border-radius:inherit;pointer-events:none;background:repeating-conic-gradient(from 0deg,rgba(255,220,120,.4) 0 4deg,transparent 4deg 16deg);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 7px),#000 calc(100% - 3px),#000 calc(100% - 1px),transparent 100%);mask:radial-gradient(farthest-side,transparent calc(100% - 7px),#000 calc(100% - 3px),#000 calc(100% - 1px),transparent 100%);filter:blur(.6px);animation:dshSkinSpin 16s linear infinite}",band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 62%,rgba(255,240,180,.5) 0%,transparent 52%)}"};
      EX_ORDER.push("19");
      EX["20"]={no:"20",name:"深海荧光",en:"ABYSS BIOLUM",group:"招牌特效",anim:"flashy",vars:{bgA:"#06222a",bgB:"#02090c",edge:"rgba(45,212,191,.4)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#d2f2ee",trk:"rgba(255,255,255,.1)",stI:"#4f7d7a",stP:"#2dd4bf",stA:"#b26bff",stD:"#5dffb0",pbgA:"#062026",pbgB:"#030c10",pbd:"rgba(45,212,191,.25)",ptx:"#d2f2ee",itm:"#8fc0ba",itD:"#5f9a85",cctx:"#8fc0ba",chipBg:"rgba(45,212,191,.06)",hintC:"#4f7d7a",hair1:"#2dd4bf",hair2:"#5dffb0"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background-image:radial-gradient(rgba(93,255,214,.95) 1.2px,transparent 1.7px),radial-gradient(rgba(178,107,255,.85) 1px,transparent 1.5px),radial-gradient(rgba(93,220,255,.8) .9px,transparent 1.4px);background-size:17px 19px,13px 15px,21px 12px;background-position:4px 8px,10px 3px,15px 14px;opacity:.8;-webkit-mask:radial-gradient(circle,#000 0 54%,transparent 74%);mask:radial-gradient(circle,#000 0 54%,transparent 74%)}"};
      EX_ORDER.push("20");
      EX["21"]={no:"21",name:"墨玉",en:"INK JADE",group:"克制精选",anim:"calm",vars:{bgA:"#22302e",bgB:"#0b1211",edge:"rgba(190,225,215,.2)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#dcf2ec",trk:"rgba(190,225,215,.14)",stI:"#5f7a74",stP:"#79a49a",stA:"#4fe0c0",stD:"#2a9d84",pbgA:"#141f1d",pbgB:"#0a100f",pbd:"rgba(190,225,215,.14)",ptx:"#d9ece7",itm:"#9db3ad",itD:"#6f938a",cctx:"#9db3ad",chipBg:"rgba(190,225,215,.07)",hintC:"#5f7a74",hair1:"#4fe0c0",hair2:"#2a9d84"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("21");
      EX["22"]={no:"22",name:"石墨极简",en:"GRAPHITE MONO",group:"克制精选",anim:"calm",vars:{bgA:"#262b33",bgB:"#101319",edge:"rgba(255,255,255,.14)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#eef1f6",trk:"rgba(255,255,255,.1)",stI:"#6b7480",stP:"#9aa2ad",stA:"#e8edf2",stD:"#ffffff",pbgA:"#181c23",pbgB:"#0d1015",pbd:"rgba(255,255,255,.09)",ptx:"#e2e7ee",itm:"#9aa4b0",itD:"#7d8792",cctx:"#9aa4b0",chipBg:"rgba(255,255,255,.05)",hintC:"#67707f",hair1:"#e8edf2",hair2:"#6b7480"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("22");
      EX["23"]={no:"23",name:"暮蓝",en:"DUSK BLUE",group:"克制精选",anim:"calm",vars:{bgA:"#1e2836",bgB:"#0b0f18",edge:"rgba(170,195,230,.2)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#dce6f4",trk:"rgba(170,195,230,.13)",stI:"#5d6b80",stP:"#8299b8",stA:"#9fc6f2",stD:"#5f86be",pbgA:"#141b26",pbgB:"#0a0e16",pbd:"rgba(170,195,230,.13)",ptx:"#d8e2f0",itm:"#9cafcc",itD:"#7288a6",cctx:"#9cafcc",chipBg:"rgba(170,195,230,.06)",hintC:"#5d6b80",hair1:"#9fc6f2",hair2:"#5f86be"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("23");
      EX["24"]={no:"24",name:"可可",en:"COCOA",group:"克制精选",anim:"calm",vars:{bgA:"#2b2019",bgB:"#120c08",edge:"rgba(224,190,150,.2)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#f2e6d8",trk:"rgba(224,190,150,.13)",stI:"#7d6a5c",stP:"#ab8d70",stA:"#e0aa6e",stD:"#a87848",pbgA:"#1c1410",pbgB:"#0f0a07",pbd:"rgba(224,190,150,.13)",ptx:"#efe2d2",itm:"#bda892",itD:"#96826e",cctx:"#bda892",chipBg:"rgba(224,190,150,.06)",hintC:"#7d6a5c",hair1:"#e0aa6e",hair2:"#a87848"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("24");
      EX["25"]={no:"25",name:"雾灰哑光",en:"MATTE MIST",group:"克制精选",anim:"calm",vars:{bgA:"#2a2f36",bgB:"#161a20",edge:"rgba(200,210,222,.18)",spec:"rgba(255,255,255,.07)",hi:"rgba(255,255,255,.06)",lbc:"#e5eaf0",trk:"rgba(200,210,222,.12)",stI:"#67707c",stP:"#8b98a8",stA:"#bcc8d6",stD:"#8b9db2",pbgA:"#181c22",pbgB:"#0e1116",pbd:"rgba(200,210,222,.12)",ptx:"#dfe4ea",itm:"#9aa5b1",itD:"#7c8894",cctx:"#9aa5b1",chipBg:"rgba(200,210,222,.06)",hintC:"#67707c",hair1:"#bcc8d6",hair2:"#67707c"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.09}"};
      EX_ORDER.push("25");
      EX["26"]={no:"26",name:"苔原",en:"MOSS TUNDRA",group:"克制精选",anim:"calm",vars:{bgA:"#1c2a20",bgB:"#0a120c",edge:"rgba(180,215,185,.2)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#ddeddd",trk:"rgba(180,215,185,.13)",stI:"#5d7a64",stP:"#86ad8c",stA:"#a4dca0",stD:"#4f9d68",pbgA:"#131f16",pbgB:"#0a110c",pbd:"rgba(180,215,185,.13)",ptx:"#dae9da",itm:"#9db8a0",itD:"#75967c",cctx:"#9db8a0",chipBg:"rgba(180,215,185,.06)",hintC:"#5d7a64",hair1:"#a4dca0",hair2:"#4f9d68"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("26");
      EX["27"]={no:"27",name:"烟紫",en:"DUSTY PLUM",group:"克制精选",anim:"calm",vars:{bgA:"#26202b",bgB:"#100b14",edge:"rgba(210,190,230,.2)",spec:"rgba(255,255,255,.16)",hi:"rgba(255,255,255,.12)",lbc:"#ebe2f4",trk:"rgba(210,190,230,.13)",stI:"#776b82",stP:"#9d8bb0",stA:"#c9aee6",stD:"#8a6cae",pbgA:"#171221",pbgB:"#0c0812",pbd:"rgba(210,190,230,.13)",ptx:"#e6ddf0",itm:"#a99bb8",itD:"#877799",cctx:"#a99bb8",chipBg:"rgba(210,190,230,.06)",hintC:"#776b82",hair1:"#c9aee6",hair2:"#8a6cae"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("27");
      EX["28"]={no:"28",name:"月白纸墨",en:"PAPER & INK",group:"克制精选",anim:"calm",vars:{bgA:"#f7f5ef",bgB:"#dcd8cd",edge:"rgba(60,55,45,.28)",spec:"rgba(255,255,255,.85)",hi:"rgba(255,255,255,.7)",lbc:"#2c2a24",trk:"rgba(60,55,45,.14)",stI:"#a09c92",stP:"#6e6a60",stA:"#33302a",stD:"#55524a",pbgA:"#faf9f5",pbgB:"#edeae1",pbd:"rgba(60,55,45,.16)",ptx:"#33302a",itm:"#6e6a60",itD:"#8b877d",cctx:"#6e6a60",chipBg:"rgba(60,55,45,.05)",hintC:"#8b877d",hair1:"#33302a",hair2:"#a09c92"},band:"%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}"};
      EX_ORDER.push("28");
      var KIND_LABEL={deep:"浓郁深底",muted:"尘埃灰调",soft:"柔光深底",neutral:"中性点缀",light:"浅色纸面"};
      var TINT="%BAND%{position:absolute;inset:0;border-radius:inherit;pointer-events:none;background:radial-gradient(circle at 50% 122%,var(--st) 0%,transparent 54%);opacity:.13}";
      var ALL=[],BY_ID={};
      (function build(){
        EX_ORDER.forEach(function(id){var s=EX[id];s.id=id;s.kind="special";ALL.push(s);BY_ID[id]=s;});
        var order=["deep","muted","soft","neutral","light"],no=28;
        order.forEach(function(kind){
          FORMULA[kind].forEach(function(row){
            no++;var id=(no<100?"0":"")+String(no);
            var s={id:id,no:id,name:row[0],en:row[1],kind:kind,group:KIND_LABEL[kind],anim:"calm",vars:varsFor(kind,row[2])};
            if(!s.band)s.band=TINT;
            ALL.push(s);BY_ID[id]=s;
          });
        });
      })();
      var KEY="dsh-ball-skin",DEFAULT_ID="01";
      function getId(){try{var v=localStorage.getItem(KEY);if(v&&BY_ID[v])return v;}catch(e){}return DEFAULT_ID;}
      function setSkin(id){
        if(!BY_ID[id])return false;
        try{localStorage.setItem(KEY,id);}catch(e){}
        var hooks=window.__dshBallSkinHooks||[];
        for(var i=0;i<hooks.length;i++){try{hooks[i](id,BY_ID[id]);}catch(e){}}
        try{window.dispatchEvent(new CustomEvent("dsh-ball-skin-change",{detail:{id:id}}));}catch(e){}
        return true;
      }
      function varsCss(sel,id){var s=BY_ID[id]||BY_ID[DEFAULT_ID];var out=sel+"{";
        for(var k in s.vars){out+="--"+k+":"+s.vars[k]+";";}
        return out+"}";}
      function varsAll(sel){return varsCss(sel,getId());}
      function fx(id,sel){var s=BY_ID[id]||BY_ID[DEFAULT_ID];var out=[];
        if(s.rim)out.push(s.rim.split("%RIM%").join(sel.RIM));
        if(s.band)out.push(s.band.split("%BAND%").join(sel.BAND));
        if(s.extra)out.push(s.extra.split("%B%").join(sel.B));
        return out.join("\n");}
      function groups(){var g=[],seen={};ALL.forEach(function(s){if(!seen[s.group]){seen[s.group]=true;g.push(s.group);}});return g;}
      window.__DSH_BALL_SKINS={ALL:ALL,BY_ID:BY_ID,getId:getId,setSkin:setSkin,varsCss:varsCss,varsAll:varsAll,fx:fx,groups:groups,KIND_LABEL:KIND_LABEL,KEY:KEY,DEFAULT_ID:DEFAULT_ID};
    })();

    // ============================================================
    // dsh-todo-float-ball — client half (browser).  v0.11.1
    //
    // A persistent, draggable floating ball that mirrors todo_write lists.
    //
    // v0.3.0 — data layer rebuilt on the official `sessions` service:
    //   * inject: ["sessions"] — the runtime's global session registry.
    //     Its list snapshot carries, for EVERY session, the projection
    //     values computed by the host (todos, title/plan/...), plus the
    //     current session id and a subscribe() for changes.
    //   * This gives us multi-session todo data and instant session-switch
    //     detection with ZERO interception — no fetch/WS wrapping (desktop
    //     renders go through the shell's __DSH_TRANSPORT__ Electron bridge,
    //     which page-level taps can never see).
    //   * Fallback: if ctx.sessions is unavailable, fall back to observing
    //     the official todo panel DOM ([data-testid="todo-panel"]).
    //
    // Features: auto-follow active conversation; pin any conversation (📌)
    // for cross-session monitoring; pinned list persists in localStorage;
    // per-status colors; Shadow DOM isolation; draggable ball.
    //
    // v0.9.1 — refresh reliability + install-and-go:
    //   * refresh fix 1: the plan-aware "progress regressed => stale snapshot"
    //     guard is GONE. todo_write rewrites the WHOLE list, so a fresh
    //     snapshot may legitimately carry fewer completed items; dropping it
    //     froze the ball on outdated data. Signature dedupe is all we need.
    //   * refresh fix 2: the 2s watcher also pulls the sessions snapshot, so
    //     a dead subscribe() callback can no longer strand the ball on old
    //     data (cheap: the DOM is only touched when something actually moved).
    //   * DEFAULTS + getCfg(): every switch has a code default, seeded into
    //     localStorage on first run — install it and it just works.
    // ============================================================

    var BALL_ID = "dsh-tfb-root";
    var POS_KEY = "dsh-todo-float-ball-pos";
    var PIN_KEY = "dsh-todo-float-ball-pinned";
    var CAPSULE_KEY = "dsh-todo-float-ball-capsule";
    var MAX_CONTENT = 80;
    var MAX_ITEMS = 100; // Plan-A history merge soft cap (see setSessionTodos)

    // ============================================================
    // Install-and-go defaults (v0.9.1)
    //
    // User requirement: "install the plugin and it just works" — no
    // configuration step. Every user-facing switch therefore carries its
    // default IN CODE below, so a machine with empty localStorage (fresh
    // install, new profile, cleared data, or an origin that changed with the
    // port) behaves exactly like a configured one.
    //
    // getCfg(key, def) reads the stored value, falls back to the code default,
    // and SEEDS the key on first run so the value stays visible/editable
    // afterwards. Seeding only ever writes a key that is ABSENT — an existing
    // user's stored choice is never overwritten.
    // ============================================================
    var DEFAULTS = {
      ballVisible: true,     // dsh-tfb-ball-visible: ball always shown
      theme: "graphite",     // dsh-tfb-theme: graphite skin
      capsule: false         // dsh-todo-float-ball-capsule: round ball, not pill
    };
    // Position default = bottom-right. The CSS already renders
    // right:18px;bottom:18px, but we also SEED the key on first run (user
    // requirement: the value must exist and stay editable). The seeded point
    // mirrors the CSS box (46px ball, 18px margin) and is clamped into the
    // viewport; the restore path re-clamps and silently falls back to the CSS
    // default when the stored point no longer fits (smaller window, or the
    // wider capsule pill), so a stale seed can never park the ball off-screen.
    var POS_BALL_SIZE = 46;
    var POS_MARGIN = 18;

    function seedValue(def) {
      if (def === true) return "1";
      if (def === false) return "0";
      return String(def);
    }
    // Pure read: the stored string, else the code default (never writes).
    function peekCfg(key, def) {
      var d = seedValue(def);
      try {
        var raw = localStorage.getItem(key);
        return (raw === null || raw === undefined) ? d : raw;
      } catch (e) { return d; }
    }
    // Read-with-seed: like peekCfg, but persists the default on first run.
    // Always returns a STRING, so callers can compare against "1"/"0" safely.
    function getCfg(key, def) {
      var d = seedValue(def);
      var raw = null;
      try { raw = localStorage.getItem(key); } catch (e) { return d; }
      if (raw !== null && raw !== undefined) return raw;
      try { localStorage.setItem(key, d); } catch (e) {}
      return d;
    }
    // Idempotent first-run seed, called once from apply().
    function ensureDefaultCfg() {
      getCfg(BALL_VIS_KEY, DEFAULTS.ballVisible);
      getCfg(THEME_KEY, DEFAULTS.theme);
      getCfg(CAPSULE_KEY, DEFAULTS.capsule);
      seedDefaultPos();
    }
    // Bottom-right corner for the CURRENT viewport, clamped so the ball is
    // always fully visible. Returns null when the document has no viewport yet
    // (headless / pre-layout), in which case we simply leave the key absent and
    // let the CSS default apply.
    function defaultPos() {
      try {
        var w = window.innerWidth || 0, h = window.innerHeight || 0;
        if (!w || !h) return null;
        return {
          x: Math.max(0, w - POS_BALL_SIZE - POS_MARGIN),
          y: Math.max(0, h - POS_BALL_SIZE - POS_MARGIN)
        };
      } catch (e) { return null; }
    }
    // Seed POS_KEY only when absent (an existing drag position is preserved).
    function seedDefaultPos() {
      try {
        var raw = localStorage.getItem(POS_KEY);
        if (raw !== null && raw !== undefined && raw !== "") return;
        var p = defaultPos();
        if (!p) return;
        localStorage.setItem(POS_KEY, JSON.stringify(p));
      } catch (e) {}
    }

    // ---------- multi-session state ----------
    var bySession = {};      // sessionId -> { todos:[{content,status}], sig }
    var titles = {};         // sessionId -> display title
    var pinned = [];         // [sessionId,...] (persisted)
    var currentSid = null;   // active session (from sessions.list.current)
    var hasSessionsSvc = false;
    var currentSessions = null;
    var expanded = false;
    var capsule = false;     // right-click: pill mode showing active task
    var openPin = {};        // pinned sessionId -> expanded? (UI state)
    // Plan-A9: fold open-state is PER-SESSION (sid-normalized maps). The
    // current session and every pinned session fold/collapse independently;
    // an absent key means collapsed (the default).
    var historyOpen = {};    // sid -> completed-history fold open?
    var historyOpenOpen = {}; // sid -> non-completed-history fold open?
    var editing = { sid: null }; // inline rename editor state (one at a time)
    // Plan-A2: contents the user hid with the row ✕. Display-only (the data
    // source is untouched); per session so it never bleeds across chats.
    var hiddenItems = {};    // sessionId -> { content:true, ... }
    var HIDE_KEY = "dsh-todo-float-ball-hidden-v1";
    var ui = { root: null, ball: null, panel: null, list: null, summary: null, ctitle: null, pinBtn: null, pinSec: null };

    function normSid(sid) {
      return (typeof sid === "string" && sid) ? sid : "current";
    }

    // ---------- pinned persistence ----------
    function loadPinned() {
      try {
        var raw = localStorage.getItem(PIN_KEY);
        if (raw) {
          var arr = JSON.parse(raw);
          if (Array.isArray(arr)) pinned = arr.filter(function (x) { return typeof x === "string" && x; });
        }
      } catch (e) { pinned = []; }
    }
    function savePinned() {
      try { localStorage.setItem(PIN_KEY, JSON.stringify(pinned)); } catch (e) {}
    }
    function isPinned(sid) { return pinned.indexOf(sid) >= 0; }

    // ---------- hidden-item set (Plan-A2) ----------
    function loadHidden() {
      try {
        var raw = localStorage.getItem(HIDE_KEY);
        if (raw) {
          var obj = JSON.parse(raw);
          if (obj && typeof obj === "object") hiddenItems = obj;
        }
      } catch (e) { hiddenItems = {}; }
    }
    function saveHidden() {
      try { localStorage.setItem(HIDE_KEY, JSON.stringify(hiddenItems)); } catch (e) {}
    }
    function hiddenFor(sid) {
      sid = normSid(sid);
      if (!hiddenItems[sid] || typeof hiddenItems[sid] !== "object") hiddenItems[sid] = {};
      return hiddenItems[sid];
    }
    // filter a list through the session's hidden set (display-only)
    function visibleList(sid, list) {
      var hid = hiddenFor(sid);
      var keys = Object.keys(hid);
      if (!keys.length || !Array.isArray(list)) return list;
      var out = [];
      for (var i = 0; i < list.length; i++) {
        if (!hid[list[i].content]) out.push(list[i]);
      }
      return out;
    }
    function hideItem(sid, content) {
      if (!content) return;
      hiddenFor(sid)[content] = true;
      saveHidden();
      render();
    }
    function showItem(sid, content) {
      var hid = hiddenFor(sid);
      if (content in hid) { delete hid[content]; saveHidden(); render(); }
    }
    function pin(sid) {
      sid = normSid(sid);
      if (isPinned(sid)) return;
      pinned.push(sid);
      savePinned();
      render();
    }
    function unpin(sid) {
      var i = pinned.indexOf(sid);
      if (i >= 0) { pinned.splice(i, 1); savePinned(); render(); }
    }

    // ---------- helpers ----------
    function esc(s) {
      return String(s)
        .replace(/&/g, "&amp;")
        .replace(/</g, "&lt;")
        .replace(/>/g, "&gt;")
        .replace(/"/g, "&quot;");
    }

    function countsOf(list) {
      var done = 0, active = 0;
      for (var i = 0; i < list.length; i++) {
        if (list[i].status === "completed") done++;
        else if (list[i].status === "in_progress") active++;
      }
      return { done: done, active: active, total: list.length };
    }

    function sessionTodos(sid) {
      var b = bySession[normSid(sid)];
      return b ? b.todos : null;
    }
    // Plan-A2.2: display variant of sessionTodos — filters the session's
    // hidden rows out. Pinned panels and their header counts use this so
    // a ✕ works identically in the current list and pinned lists.
    function sessionTodosVisible(sid) {
      return visibleList(normSid(sid), sessionTodos(sid));
    }
    function sessionTitle(sid) {
      sid = normSid(sid);
      if (titles[sid]) return titles[sid];
      return sid === "current" ? "" : sid.slice(0, 10) + "…";
    }

    // ---------- rename (official API) ----------
    // Path (verified against dsh-client-runtime): the sessions facade exposes
    // .manager; SessionManager lazily builds resident Session records
    // (.sessions Map, .get(sessionId) is side-effect-safe); each Session has
    // .rename(title) → api.sessions.rename → host normalizes → title
    // projection updates → our sessions.list subscription re-renders.
    function renameSession(sid, title) {
      sid = normSid(sid);
      var clean = (typeof title === "string" ? title.trim() : "");
      if (!clean) return Promise.resolve(false);
      try {
        var mgr = currentSessions && currentSessions.manager;
        var session = mgr && mgr.sessions && typeof mgr.sessions.get === "function" ? mgr.sessions.get(sid) : null;
        if (session && typeof session.rename === "function") {
          return Promise.resolve(session.rename(clean)).then(function (result) {
            if (result && result.ok) {
              titles[sid] = (result.value && result.value.title) || clean;
              render();
              return true;
            }
            render();
            return false;
          }).catch(function () { render(); return false; });
        }
      } catch (e) {}
      return Promise.resolve(false);
    }

    function startRename(sid) {
      if (!hasSessionsSvc) return;
      sid = normSid(sid);
      if (editing.sid) return; // one editor at a time
      var isCurrent = sid === normSid(currentSid);
      var host = isCurrent ? ui.ctitle
        : (ui.pinSec ? ui.pinSec.querySelector('.tfb-pinrow[data-sid="' + sid + '"] .tfb-pinname') : null);
      if (!host) return;
      editing.sid = sid;
      var old = sessionTitle(sid);
      host.innerHTML = '<input class="tfb-rename" data-rename="1" value="' + esc(old) + '" placeholder="会话名称">';
      var input = host.querySelector("input");
      if (!input) { editing.sid = null; return; }
      input.focus();
      try { input.select(); } catch (e) {}
      var done = false;
      var commit = function () {
        if (done) return; done = true;
        var v = input.value;
        editing.sid = null;
        if (v && v.trim() && v.trim() !== old) renameSession(sid, v);
        else render();
      };
      var cancel = function () {
        if (done) return; done = true;
        editing.sid = null;
        render();
      };
      input.addEventListener("keydown", function (e) {
        if (e.key === "Enter") { e.preventDefault(); commit(); }
        else if (e.key === "Escape") { e.preventDefault(); cancel(); }
      });
      input.addEventListener("blur", commit);
      input.addEventListener("click", function (e) { e.stopPropagation(); });
      input.addEventListener("pointerdown", function (e) { e.stopPropagation(); });
    }

    // Plan-A2: display list = stored list minus the user's hidden rows
    // (panel list and summary read this).
    function currentList() { return visibleList(currentSid, sessionTodos(currentSid)) || []; }
    // Plan-A4 (user-approved): the ball face, progress ring, header summary
    // and the "items left" hint report the CURRENT SNAPSHOT only — the AI's
    // actual live plan. Accumulated history (the folds) stays out of every
    // counter: it is an archive, not progress.
    // Plan-A9: general-sid snapshot slice — the session's visible merged
    // list cut at its snapLen (history folds live strictly after it). The
    // ONE source of truth for every "snapshot only" counter (A4 rule),
    // current session and pinned sessions alike.
    function snapshotRowsOf(sid) {
      var nsid = normSid(sid);
      var raw = sessionTodos(nsid) || [];
      var b = bySession[nsid];
      var snapLen = (b && typeof b.snapLen === "number") ? Math.min(b.snapLen, raw.length) : raw.length;
      return visibleList(nsid, raw.slice(0, snapLen));
    }
    function snapshotList() { return snapshotRowsOf(currentSid); }

    function summarize(list) {
      var c = countsOf(list);
      var head = c.done + "/" + c.total;
      var detail = "";
      if (c.active > 0) {
        var first = null;
        for (var i = 0; i < list.length; i++) {
          if (list[i].status === "in_progress") { first = list[i].content; break; }
        }
        if (first) detail = first.length > 30 ? first.slice(0, 30) + "…" : first;
        if (c.active > 1) detail += " (+" + (c.active - 1) + ")";
      }
      return { head: head, detail: detail, c: c };
    }

    function ballState() {
      var list = snapshotList(); // Plan-A4: current snapshot only
      if (!list.length) return { cls: "tfb-idle", label: "✓" };
      var c = countsOf(list);
      if (c.done === c.total) return { cls: "tfb-done", label: "✓" };
      return { cls: c.active > 0 ? "tfb-active" : "tfb-pending", label: String(c.done) + "/" + String(c.total) };
    }

    // ---------- durable persistence (user requirement: cross-turn /
    // cross-refresh record survival) ----------
    // Every accepted snapshot is written to localStorage keyed by session.
    // On restore we take whichever side is "further along" (more completed
    // items, tie-break by total, then by recency) so nothing — a stale
    // snapshot poll, a page reload, a projection rebuild — can drag the
    // record BACKWARD. Completed items are history, not transients.
    var PERSIST_KEY = "dsh-todo-float-ball-persist-v1";
    var persistTimer = null;
    function loadPersisted() {
      var out = {};
      try {
        var raw = localStorage.getItem(PERSIST_KEY);
        if (raw) out = JSON.parse(raw) || {};
      } catch (e) { out = {}; }
      return out;
    }
    function persistSoon() {
      if (persistTimer) return;
      persistTimer = setTimeout(function () {
        persistTimer = null;
        try {
          var out = {};
          for (var sid in bySession) {
            var b = bySession[sid];
            if (b && b.todos && b.todos.length) {
              out[sid] = { todos: b.todos, sig: b.sig, at: b.at || 0, snapLen: b.snapLen || 0 };
            }
          }
          localStorage.setItem(PERSIST_KEY, JSON.stringify(out));
        } catch (e) {}
      }, 400);
    }
    // progress score: completed count dominates, then total, then recency.
    // planKey: the sorted content set — identical set = same plan (progress
    // within one plan must never regress); different set = a new plan.
    function planKey(todos) {
      return todos.map(function (t) { return t.content; }).sort().join("\n");
    }
    function progressScore(todos, at) {
      var c = countsOf(todos);
      return c.done * 10000 + c.total * 100 + Math.min(99, Math.floor(((at || 0) / 60000) % 100));
    }
    function restorePersisted() {
      try {
        var saved = loadPersisted();
        for (var sid in saved) {
          var rec = saved[sid];
          if (!rec || !Array.isArray(rec.todos) || rec.todos.length === 0) continue;
          var norm = [];
          for (var i = 0; i < rec.todos.length; i++) {
            var it = rec.todos[i];
            if (it && typeof it.content === "string" && it.content.trim()) {
              norm.push({ content: it.content, status: it.status });
            }
          }
          if (!norm.length) continue;
          var mem = bySession[sid];
          if (mem && mem.todos && mem.todos.length) {
            // different plan: the live snapshot wins (it is what the host
            // currently holds); same plan: keep the further-along record
            if (planKey(mem.todos) !== planKey(norm)) continue;
            if (progressScore(norm, rec.at) <= progressScore(mem.todos, mem.at)) continue;
          }
          bySession[sid] = { todos: norm, sig: rec.sig || norm.map(function (t) { return t.status + "|" + t.content; }).join("\n"), stale: false, at: rec.at || 0, snapLen: rec.snapLen || norm.length };
        }
      } catch (e) {}
    }

    // ---------- data ingestion ----------
    function setSessionTodos(sid, list) {
      sid = normSid(sid);
      if (!Array.isArray(list)) return false; // false = nothing changed
      var norm = [];
      for (var i = 0; i < list.length; i++) {
        var it = list[i];
        if (!it || typeof it !== "object") continue;
        var st = it.status;
        if (st !== "pending" && st !== "in_progress" && st !== "completed") continue;
        var content = typeof it.content === "string" ? it.content : "";
        if (!content.trim()) continue;
        norm.push({ content: content, status: st });
      }
      if (norm.length === 0) {
        // STICKY: the host resets the todos projection to null at every
        // turn/start. Never wipe the last known list — only a real
        // todo_write snapshot (or a strictly-more-complete persisted one)
        // changes it. Per-session bucketing keeps this from bleeding
        // across conversations.
        var b0 = bySession[sid];
        if (b0) b0.stale = true;
        return false; // sticky: an empty payload never wipes the record
      }
      var sig = norm.map(function (t) { return t.status + "|" + t.content; }).join("\n");
      var b = bySession[sid];
      // Signature dedupe FIRST (v0.9.1 semantics, unchanged): an identical
      // snapshot from the 2s safety-net poll or a re-subscribe must stay a
      // cheap no-op — in particular it must NEVER undo the user's ✕ hides.
      // Plan-A2 auto-resurface therefore runs only AFTER we know this is a
      // genuinely NEW snapshot (a real todo_write): if the model explicitly
      // lists a previously hidden item again, that row is live again and
      // un-hides. The ✕ keeps silencing rows that merely sit in the merged
      // history / unchanged snapshot.
      if (b && b.sig === sig) { b.stale = false; return false; }
      var hid0 = hiddenFor(sid);
      for (var h0 in hid0) {
        for (var ni0 = 0; ni0 < norm.length; ni0++) {
          if (norm[ni0].content === h0) { delete hid0[h0]; break; }
        }
      }
      saveHidden();
      // NO progress-regression guard (removed in v0.9.1 on a user bug report).
      // todo_write is a WHOLE-LIST replacement, so a fresh snapshot may
      // legitimately carry FEWER completed items than the last one rendered:
      // the list was re-organised, a status was re-adjusted, an item was
      // re-opened, or a round rewrote the plan. The old plan-aware guard
      // compared done-counts within an identical content set and silently
      // dropped those snapshots as "stale", which is exactly why the ball kept
      // showing outdated progress. A new snapshot now ALWAYS wins; the
      // signature check above is the only dedupe (unchanged input costs
      // nothing, so no redundant work).
      // Plan-A history merge (2026-09-13, user approved): the snapshot is a
      // whole-list replacement, so simply overwriting bySession[sid] made
      // earlier tasks vanish whenever a new round rewrote the list. Merge
      // instead: the fresh snapshot leads (its statuses win), and items that
      // only exist in the previous record are appended in their last known
      // state — history never disappears. A soft cap keeps the list bounded.
      var merged = [];
      var seen = {};
      for (var mi = 0; mi < norm.length; mi++) {
        seen[norm[mi].content] = true;
        merged.push(norm[mi]);
      }
      // (auto-resurface now runs before the signature dedupe; see Plan-A2 note)
      var prev = (b && b.todos) ? b.todos : [];
      for (var pi = 0; pi < prev.length; pi++) {
        if (!seen[prev[pi].content]) {
          seen[prev[pi].content] = true;
          merged.push(prev[pi]);
        }
      }
      // soft cap: keep the ENTIRE snapshot (it leads) plus the most recent
      // history entries that still fit — oldest history overflows first.
      // (A snapshot larger than the cap itself is kept in full: it is what
      // the host currently holds, truncating it would hide live tasks.)
      if (merged.length > MAX_ITEMS && norm.length < MAX_ITEMS) {
        var histQuota = MAX_ITEMS - norm.length;
        merged = norm.concat(merged.slice(Math.max(norm.length, merged.length - histQuota)));
      }
      // Plan-A3 (user-approved option ③): remember where the fresh snapshot
      // ends. Everything after snapLen is accumulated history — rendered
      // collapsed (completed-only) by renderList; the snapshot itself stays
      // fully visible.
      bySession[sid] = { todos: merged, sig: sig, stale: false, at: Date.now(), snapLen: norm.length };
      render();
      persistSoon();
      return true; // a new snapshot was accepted -> the caller must re-render
    }

    // Primary channel: the official sessions service.
    // list snapshot shape (from dsh-client-runtime):
    //   { ids:[...], byId:{ sid:{ id, displayTitle, running, projectionValues:{todos,title,...}, ... } },
    //     current: sid|undefined, phase }
    function syncFromSessions() {
      try {
        if (!currentSessions || !currentSessions.list || typeof currentSessions.list.getSnapshot !== "function") return;
        var snap = currentSessions.list.getSnapshot();
        if (!snap || !snap.byId) return;
        var changed = false;
        for (var id in snap.byId) {
          var rec = snap.byId[id];
          if (!rec) continue;
          if (typeof rec.displayTitle === "string" && rec.displayTitle && titles[id] !== rec.displayTitle) {
            titles[id] = rec.displayTitle;
            changed = true;
          }
          var pv = rec.projectionValues;
          if (pv && typeof pv === "object" && "todos" in pv) {
            if (setSessionTodos(id, pv.todos === null ? [] : pv.todos)) changed = true;
          }
        }
        if (snap.current && snap.current !== currentSid) {
          currentSid = snap.current;
          changed = true;
        }
        // Only touch the DOM when something actually moved. This is what lets
        // the 2s safety-net poll be effectively free while nothing changes
        // (the settings/theme skin switches still re-render via render()).
        if (changed) render();
      } catch (e) {}
    }

    // Fallback channel: observe the official todo panel DOM. Expanded panel
    // renders ul > li[data-status]; collapsed renders only header counts
    // (parsed into a skeleton — better than nothing when sessions service
    // is unavailable).
    function installPanelObserver() {
      var mo = new MutationObserver(function () {
        if (installPanelObserver._t) return;
        installPanelObserver._t = setTimeout(function () {
          installPanelObserver._t = 0;
          try { readOfficialPanel(); } catch (e) {}
        }, 200);
      });
      function start() {
        try {
          mo.observe(document.documentElement || document.body, { childList: true, subtree: true });
          readOfficialPanel();
        } catch (e) {}
      }
      if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", start);
      else start();
    }

    var ZH_NUM = { "零": 0, "一": 1, "二": 2, "两": 2, "三": 3, "四": 4, "五": 5, "六": 6, "七": 7, "八": 8, "九": 9, "十": 10 };

    function parseZhNum(s) {
      s = String(s).trim();
      if (/^\d+$/.test(s)) return parseInt(s, 10);
      if (ZH_NUM[s] !== undefined) return ZH_NUM[s];
      if (/^十$/.test(s)) return 10;
      var m = /^([一二两三四五六七八九])?十([一二三四五六七八九])?$/.exec(s);
      if (m) return (m[1] ? ZH_NUM[m[1]] : 1) * 10 + (m[2] ? ZH_NUM[m[2]] : 0);
      return NaN;
    }

    function readOfficialPanel() {
      var panel = document.querySelector('[data-testid="todo-panel"]');
      if (!panel) return;
      var items = panel.querySelectorAll("ul li[data-status]");
      if (items.length > 0) {
        var list = [];
        for (var i = 0; i < items.length; i++) {
          var li = items[i];
          var st = li.getAttribute("data-status");
          var contentEl = li.querySelector("span:last-child") || li.lastElementChild;
          var content = contentEl ? (contentEl.textContent || "").trim() : "";
          if (content) list.push({ content: content, status: st || "pending" });
        }
        if (list.length > 0) { setSessionTodos(currentSid, list); return; }
      }
      // collapsed: skeleton only if the current session has no real data
      var cur = bySession[normSid(currentSid)];
      var hasReal = cur && cur.todos && cur.todos.length > 0 && !cur.__skeleton;
      if (hasReal) return;
      var label = panel.querySelector('[class*="progress"]');
      if (!label) return;
      var text = (label.textContent || "").trim();
      if (!text) return;
      var segs = text.split("·");
      var done = NaN, active = NaN, pending = NaN;
      for (var k = 0; k < segs.length; k++) {
        var s = segs[k].trim();
        var n = parseZhNum(s.split(" ")[0]);
        if (isNaN(n)) continue;
        if (s.indexOf("完成") >= 0) done = n;
        else if (s.indexOf("进行") >= 0) active = n;
        else if (s.indexOf("待办") >= 0) pending = n;
      }
      if (isNaN(done) && isNaN(active) && isNaN(pending)) return;
      // only show the skeleton when we have nothing real, and mark it
      if (bySession[normSid(currentSid)] && !bySession[normSid(currentSid)].__skeleton) return;
      var skel = [];
      for (var d = 0; d < (isNaN(done) ? 0 : done); d++) skel.push({ content: "已完成任务", status: "completed" });
      for (var a = 0; a < (isNaN(active) ? 0 : active); a++) skel.push({ content: "进行中任务", status: "in_progress" });
      for (var p = 0; p < (isNaN(pending) ? 0 : pending); p++) skel.push({ content: "待办任务", status: "pending" });
      if (skel.length > 0) {
        setSessionTodos(currentSid, skel);
        var bb = bySession[normSid(currentSid)];
        if (bb) bb.__skeleton = true;
        render();
      }
    }

    // ---------- UI ----------
    // ---------- theme (dual skin, user requirement) ----------
    // "nebula"  — skill-orb style clone (v0.6.2): nebula body + flowing
    //             bands + rotating conic rim + breathing + glow text.
    // "graphite" — v0.5.1 dark-gem style: same glass family as
    //             font-enhancer, teal accent + thin progress ring.
    // Persisted in localStorage; switched from the shared "悬浮球导航"
    // settings section (dsh-skill-browser provides the button).
    var THEME_KEY = "dsh-tfb-theme";
    var currentTheme = DEFAULTS.theme;
    var currentSkinId = null;   // active unified skin (see skin system below)
    var legacyPicked = false;   // user explicitly chose a classic skin
    // single source of truth for "is this a known skin?"
    function isTheme(t) {
      return t === "nebula" || t === "graphite" || t === "blue" || t === "glass" || t === "nebB" || t === "nebC";
    }
    function loadTheme() {
      // stored value wins; absent => seeded default; unknown => default
      var v = getCfg(THEME_KEY, DEFAULTS.theme);
      currentTheme = isTheme(v) ? v : DEFAULTS.theme;
      try {
        var raw = readSharedKey();
        if (raw && SK && SK.BY_ID[raw]) currentSkinId = raw;
        else if (!raw && SK) currentSkinId = SK.DEFAULT_ID;
        else if (raw === "legacy") currentSkinId = null;
      } catch (e) {}
    }
    function setTheme(t) {
      if (t !== "nebula" && t !== "graphite" && t !== "blue" && t !== "glass" && t !== "nebB" && t !== "nebC") return;
      currentTheme = t;
      try { localStorage.setItem(THEME_KEY, t); } catch (e) {}
      render();
    }

    // ---------- unified 128-skin system (shared with skill/font balls) ----------
    // Catalog: window.__DSH_BALL_SKINS (proposal ids 01-128, matches the
    // approved design sheets). Legacy 6 skins stay selectable as 经典.
    var SK = window.__DSH_BALL_SKINS || null;
    var SHARED_KEY = SK ? SK.KEY : "dsh-ball-skin";
    function readSharedKey() { try { return localStorage.getItem(SHARED_KEY); } catch (e) { return null; } }
    function skinCssText() {
      var s = (SK && currentSkinId && SK.BY_ID[currentSkinId]) ? SK.BY_ID[currentSkinId] : null;
      if (!s) return legacyCssText();
      var V = [];
      for (var k in s.vars) V.push("--" + k + ":" + s.vars[k]);
      V.push("--pline:var(--hair2,rgba(255,255,255,.2))");
      var ball = [
        ":host{all:initial}",
        ".tfb-root{position:static;pointer-events:none;font-family:'Segoe UI','Microsoft YaHei',system-ui,sans-serif;" + V.join(";") + "}",
        ".tfb-ball:hover{transform:scale(1.08)}",
        ".tfb-ball:active{cursor:grabbing;transform:scale(.95)}",
        ".tfb-ball{position:fixed;right:18px;bottom:18px;z-index:2147483600;border-radius:50%;border:1px solid var(--edge,rgba(255,255,255,.14));cursor:grab;user-select:none;touch-action:none;pointer-events:auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;color:var(--lbc,#eef1f6);font-size:13px;font-weight:800;line-height:1.1;font-variant-numeric:tabular-nums;width:46px;height:46px;background:radial-gradient(circle at 32% 24%,var(--spec,rgba(255,255,255,.16)) 0%,rgba(255,255,255,.03) 28%,rgba(255,255,255,0) 46%),linear-gradient(158deg,var(--bgA,#262b33),var(--bgB,#101319));box-shadow:inset 0 1px 0 var(--hi,rgba(255,255,255,.12)),0 5px 14px rgba(0,0,0,.42);transition:transform .15s ease}",
        ".tfb-ball.tfb-idle{--st:var(--stI,#8fa0b8)}",
        ".tfb-ball.tfb-pending{--st:var(--stP,#6fb6ff)}",
        ".tfb-ball.tfb-active{--st:var(--stA,#ffb454)}",
        ".tfb-ball.tfb-done{--st:var(--stD,#34e39a)}",
        ".tfb-ball .tfb-label{position:relative;z-index:2;text-shadow:0 0 6px var(--st,transparent)}",
        ".tfb-ball .tfb-ring{position:absolute;inset:-1px;border-radius:50%;pointer-events:none;background:conic-gradient(var(--st,transparent) calc(var(--pct,0)*1%),var(--trk,rgba(255,255,255,.1)) 0);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 4px),#000 calc(100% - 3px));mask:radial-gradient(farthest-side,transparent calc(100% - 4px),#000 calc(100% - 3px));transition:background .4s ease}",
        ".tfb-ball.tfb-idle .tfb-ring{opacity:.55}",
        ".tfb-ball .tfb-orb-band{position:absolute;inset:0;border-radius:inherit;pointer-events:none}",
        ".tfb-ball.tfb-active .tfb-orb-rim{animation-duration:2.6s}"
      ];
      if (s.anim === "flashy") {
        ball.push(".tfb-ball.tfb-active{animation:tfb-pulse 1.8s ease-in-out infinite}");
      }
      var fxc = SK.fx(currentSkinId, { B: ".tfb-ball", BAND: ".tfb-ball .tfb-orb-band", RIM: ".tfb-ball .tfb-orb-rim" });
      if (fxc) ball.push(fxc);
      if (!s.rim) ball.push(".tfb-ball .tfb-orb-rim{display:none}");
      if (!s.band) ball.push(".tfb-ball .tfb-orb-band{display:none}");
      ball.push(".tfb-ball.tfb-capsule .tfb-orb-band{border-radius:22px}");
      ball.push(
        // ---- panel (var-driven, fallbacks = classic look) ----
        '@keyframes dshSkinSpin{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}',
        '@keyframes dshSkinSpinR{0%{transform:rotate(0deg)}100%{transform:rotate(-360deg)}}',
        '@keyframes tfb-pulse{0%,100%{box-shadow:inset 0 1px 0 var(--hi,rgba(255,255,255,.12)),0 5px 14px rgba(0,0,0,.42),0 0 14px var(--st,rgba(255,170,51,.45)),0 0 0 0 var(--st,rgba(255,170,51,.5))}50%{box-shadow:inset 0 1px 0 var(--hi,rgba(255,255,255,.12)),0 5px 14px rgba(0,0,0,.42),0 0 22px var(--st,rgba(255,170,51,.55)),0 0 0 9px rgba(255,170,51,0)}}',
        ".tfb-panel{position:fixed;z-index:2147483600;width:300px;max-width:calc(100vw - 24px);max-height:74vh;overflow-y:auto;overflow-x:hidden;pointer-events:auto;display:none;color:var(--ptx,#e6e6e6);font-size:13px;line-height:1.5;border-radius:14px;padding:12px 14px 10px;background:linear-gradient(180deg,var(--pbgA,rgba(16,18,22,.94)),var(--pbgB,rgba(12,15,24,.96)));border:1px solid var(--pbd,rgba(255,255,255,.08));box-shadow:0 20px 60px rgba(0,0,0,.7),inset 0 1px 0 var(--hi,rgba(255,255,255,.06));backdrop-filter:blur(24px)}",
        ".tfb-panel::before{content:'';position:absolute;top:0;left:16px;right:16px;height:1px;pointer-events:none;background:linear-gradient(90deg,transparent,var(--hair1,#3ec6ff),var(--hair2,#7c6bff),transparent);opacity:.75}",
        ".tfb-panel.tfb-open{display:block}",
        ".tfb-panel.tfb-open.tfb-hz{display:flex;flex-wrap:wrap;align-items:flex-start;width:auto;min-width:540px;max-width:min(92vw,800px)}",
        ".tfb-panel.tfb-hz .tfb-head{flex:0 0 100%;width:100%}",
        ".tfb-colcap{display:none;font-size:11px;font-weight:600;color:var(--stA,#8fd3c0);padding:2px 0 6px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}",
        ".tfb-panel.tfb-hz .tfb-colcap{display:block}",
        ".tfb-panel.tfb-hz .tfb-col-cur{flex:1 1 45%;min-width:250px;max-width:none;box-sizing:border-box;padding-right:10px;border-right:1px solid var(--pbd,rgba(255,255,255,.1))}",
        ".tfb-panel.tfb-hz .tfb-col-pin{flex:1 1 50%;min-width:260px;box-sizing:border-box;padding-left:10px}",
        ".tfb-head{display:flex;align-items:center;gap:6px;margin-bottom:6px;padding-bottom:6px;border-bottom:1px solid var(--pline,rgba(45,212,168,.25))}",
        ".tfb-title{font-weight:700;font-size:13px;white-space:nowrap}",
        ".tfb-ctitle{flex:1;min-width:0;font-size:11px;color:var(--itm,#9aa3b2);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;cursor:text}",
        ".tfb-summary{font-size:11px;color:var(--itm,#9aa3b2);white-space:nowrap}",
        ".tfb-btn{border:0;background:transparent;color:var(--cctx,#9aa3b2);font-size:13px;cursor:pointer;padding:2px 5px;border-radius:6px;flex-shrink:0}",
        ".tfb-btn:hover{color:var(--ptx,#e6e6e6);background:var(--chipBg,rgba(255,255,255,.06))}",
        ".tfb-btn.tfb-on{color:var(--stA,#ffd27a)}",
        ".tfb-list{list-style:none;margin:0;padding:0}",
        ".tfb-item{display:flex;align-items:flex-start;gap:8px;padding:5px 4px;border-radius:8px;word-break:break-word}",
        ".tfb-item:nth-child(odd){background:var(--chipBg,rgba(255,255,255,.03))}",
        ".tfb-ico{flex-shrink:0;width:16px;height:16px;border-radius:50%;display:inline-flex;align-items:center;justify-content:center;font-size:10px;margin-top:1px}",
        ".tfb-item[data-status='completed'] .tfb-ico{background:var(--chipBg,rgba(74,222,128,.18));color:var(--stD,#5dffa8);border:1px solid var(--stD,#5dffa8)}",
        ".tfb-item[data-status='in_progress'] .tfb-ico{background:var(--chipBg,rgba(255,170,51,.2));color:var(--stA,#ffaa33);border:1px solid var(--stA,#ffaa33)}",
        ".tfb-item[data-status='pending'] .tfb-ico{background:transparent;color:var(--stP,#7fc4ff);border:1px dashed var(--stP,#7fc4ff)}",
        ".tfb-item[data-status='in_progress']{background:var(--chipBg,rgba(255,170,51,.06));box-shadow:inset 2px 0 0 var(--stA,#ffaa33)}",
        ".tfb-item[data-status='in_progress'] .tfb-txt{color:var(--stA,#ffcf8a);font-weight:600}",
        ".tfb-item[data-status='completed'] .tfb-txt{color:var(--itD,#7fae94);text-decoration:line-through}",
        ".tfb-item[data-status='pending'] .tfb-txt{color:var(--itm,#aebfd4)}",
        ".tfb-txt{flex:1}",
        ".tfb-x{flex-shrink:0;border:0;background:transparent;color:var(--hintC,#6b7280);font-size:11px;line-height:1;cursor:pointer;padding:2px 4px;border-radius:5px;opacity:0;transition:opacity .12s ease}",
        ".tfb-item:hover .tfb-x,.tfb-x:focus{opacity:1}",
        ".tfb-x:hover{color:#ff8a8a;background:rgba(255,120,120,.12)}",
        ".tfb-empty{color:var(--itm,#8b93a1);font-size:12px;text-align:center;padding:12px 0}",
        ".tfb-histoggle{list-style:none;display:flex;align-items:center;justify-content:center;gap:4px;margin:6px 0 2px;padding:5px 4px;border-radius:8px;color:var(--itm,#9aa3b2);font-size:11px;cursor:pointer;background:var(--chipBg,rgba(255,255,255,.03))}",
        ".tfb-histoggle:hover{color:var(--ptx,#e6e6e6);background:var(--chipBg,rgba(255,255,255,.06))}",
        ".tfb-sep{margin:10px 0 6px;padding-top:8px;border-top:1px solid var(--pbd,rgba(255,255,255,.07));display:flex;align-items:center;justify-content:space-between}",
        ".tfb-sep-label{font-size:11px;color:var(--itm,#9aa3b2);font-weight:600}",
        ".tfb-pinrow{display:flex;align-items:center;gap:6px;padding:6px 4px;border-radius:8px;cursor:pointer}",
        ".tfb-pinrow:hover{background:var(--chipBg,rgba(255,255,255,.05))}",
        ".tfb-pinname{flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px}",
        ".tfb-pincount{font-size:11px;color:var(--itm,#9aa3b2);white-space:nowrap}",
        ".tfb-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0}",
        ".tfb-dot-active{background:var(--stA,#fbbf24);box-shadow:0 0 6px var(--stA,rgba(251,191,36,.8));animation:tfb-dotpulse 1.4s ease-in-out infinite}",
        ".tfb-dot-done{background:var(--stD,#4ade80)}",
        ".tfb-dot-none{background:var(--hintC,#6b7280)}",
        "@keyframes tfb-dotpulse{0%,100%{opacity:1}50%{opacity:.4}}",
        ".tfb-pinlist{list-style:none;margin:2px 0 4px;padding:0 0 0 10px;border-left:2px solid var(--pbd,rgba(255,255,255,.08))}",
        ".tfb-hint{font-size:10px;color:var(--hintC,#6b7280);padding:6px 0 0;display:flex;align-items:center;justify-content:center;gap:4px;flex-wrap:wrap}",
        ".tfb-theme-menu{display:none;margin:-2px 0 8px;padding:6px;border:1px solid var(--pline,rgba(45,212,168,.25));border-radius:10px;background:var(--pbgB,rgba(12,16,20,.96));max-height:46vh;overflow-y:auto}",
        ".tfb-theme-menu.tfb-open{display:block}",
        ".tfb-theme-cap{font-size:10px;color:var(--hintC,#6b7280);padding:6px 8px 2px;font-weight:700;letter-spacing:1px}",
        ".tfb-theme-row{display:flex;align-items:center;gap:8px;padding:5px 8px;border-radius:8px;cursor:pointer;font-size:12px;color:var(--ptx,#cdd6e2)}",
        ".tfb-theme-row:hover{background:var(--chipBg,rgba(255,255,255,.06))}",
        ".tfb-theme-row.tfb-cur{color:var(--stA,#ffd27a);font-weight:600}",
        ".tfb-theme-row .tfb-theme-check{flex-shrink:0;width:14px;text-align:center}",
        ".tfb-theme-row .tfb-swdot{display:inline-block;width:10px;height:10px;border-radius:50%;flex-shrink:0;border:1px solid var(--pbd,rgba(255,255,255,.2))}",
        ".tfb-theme-row .tfb-theme-label{flex:1;min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}",
        ".tfb-rename{flex:1;min-width:0;padding:3px 8px;border-radius:6px;border:1px solid var(--stA,rgba(255,210,122,.5));background:rgba(0,0,0,.4);color:var(--stA,#ffd27a);font:inherit;font-size:12px;outline:none}",
        ".tfb-rename:focus{border-color:var(--stA,#ffd27a)}",
        ".tfb-ctitle[contenteditable='true']{outline:none;border-bottom:1px dashed var(--stA,#ffd27a);color:var(--stA,#ffd27a);cursor:text}",
        // capsule mode (right-click toggle)
        ".tfb-ball.tfb-capsule{width:auto;min-width:220px;max-width:min(520px,60vw);height:44px;border-radius:22px;flex-direction:row;gap:8px;padding:0 16px;font-size:12px;justify-content:flex-start}",
        ".tfb-ball.tfb-capsule .tfb-orb-band{inset:0}",
        ".tfb-ball.tfb-capsule .tfb-orb-rim{display:none}",
        ".tfb-ball.tfb-capsule .tfb-ring{display:none}",
        ".tfb-ball.tfb-capsule .tfb-label{flex-shrink:0}",
        ".tfb-ball.tfb-capsule .tfb-sub{flex:1;min-width:0;max-width:none;font-size:12px;text-align:left;overflow:hidden;white-space:nowrap}",
        ".tfb-ball.tfb-capsule .tfb-capsule-text{display:inline-block;white-space:nowrap}",
        ".tfb-ball.tfb-capsule .tfb-capsule-text.tfb-marquee{animation:tfbMarquee 9s ease-in-out infinite alternate}",
        "@keyframes tfbMarquee{from{transform:translateX(0)}to{transform:translateX(var(--shift,-40px))}}",
        // ---- unified typography (three-ball spec) ----
        ".tfb-root,.tfb-panel{font-family:'Segoe UI','Microsoft YaHei','PingFang SC',system-ui,sans-serif}",
        ".tfb-panel{font-size:13px;line-height:1.55;font-variant-numeric:tabular-nums}",
        ".tfb-title{font-size:13px}",
        ".tfb-ctitle,.tfb-summary,.tfb-sep-label,.tfb-pincount,.tfb-colcap{font-size:11.5px}",
        ".tfb-btn{font-size:12.5px}",
        ".tfb-txt{font-size:12.5px}",
        ".tfb-theme-row,.tfb-pinname,.tfb-empty,.tfb-histoggle{font-size:12px}",
        ".tfb-theme-cap,.tfb-hint{font-size:10.5px}"
      );
      return ball.join("\n");
    }
    function cssText() {
      return (SK && currentSkinId && SK.BY_ID[currentSkinId]) ? skinCssText() : legacyCssText();
    }

    function legacyCssText() {
      var th = currentTheme;
      // Skeleton fields that all four skins share; shape differs per skin.
      var SIZE = "width:46px;height:46px;";
      // Per-skin ball rules (status variants + ring + band/rim).
      var ball = [];
      if (th === "nebula") {
        ball = [
          ".tfb-ball{position:fixed;right:18px;bottom:18px;z-index:2147483600;border-radius:50%;border:1px solid rgba(255,255,255,.28);cursor:grab;user-select:none;touch-action:none;pointer-events:auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;color:#eafff7;font-size:13px;font-weight:800;line-height:1.1;background:radial-gradient(circle at 50% 60%, #2dd4a8 0%, #17a284 20%, #12876f 45%, #0a3a32 68%, #041715 100%);box-shadow:0 0 22px rgba(45,212,168,.5),0 0 4px rgba(120,255,214,.35),0 4px 18px rgba(0,0,0,.55),inset 0 1px 3px rgba(255,255,255,.4),inset 0 -8px 16px rgba(45,212,168,.16),inset 0 0 14px rgba(20,180,150,.25);text-shadow:0 0 6px rgba(120,255,214,.75),0 2px 4px rgba(0,40,30,.8);filter:drop-shadow(0 0 6px rgba(160,255,225,.85));animation:tfbBreathe 3.2s ease-in-out infinite;transition:transform .15s ease}" + SIZE.replace(";", ";"),
          ".tfb-orb-band{display:none}",
          ".tfb-orb-rim{position:absolute;inset:-1px;border-radius:50%;z-index:1;pointer-events:none;background:conic-gradient(from 200deg, rgba(255,255,255,.55), rgba(120,240,205,.35), rgba(10,200,170,.4), rgba(255,255,255,.15), rgba(45,212,168,.4), rgba(255,255,255,.55));-webkit-mask:radial-gradient(circle, transparent 63%, #000 66%, #000 72%, transparent 76%);mask:radial-gradient(circle, transparent 63%, #000 66%, #000 72%, transparent 76%);animation:tfbRimSpin 7s linear infinite;filter:blur(.5px)}",
          ".tfb-ball .tfb-ring{display:none}",
          ".tfb-ball .tfb-label{animation:tfbGlow 2.4s ease-in-out infinite}",
          ".tfb-ball.tfb-active{color:#fff3dc;background:radial-gradient(circle at 50% 60%, #ffaa33 0%, #e08410 20%, #b56708 45%, #4a2a06 70%, #170c02 100%);box-shadow:0 0 22px rgba(255,170,51,.55),0 0 4px rgba(255,210,120,.4),0 4px 18px rgba(0,0,0,.55),inset 0 1px 3px rgba(255,255,255,.4),inset 0 -8px 16px rgba(255,170,51,.16),inset 0 0 14px rgba(230,140,20,.25);text-shadow:0 0 4px rgba(255,255,255,.95),0 0 10px rgba(255,215,140,.95),0 0 20px rgba(255,160,40,.8),0 2px 4px rgba(60,30,0,.8);filter:drop-shadow(0 0 6px rgba(255,200,110,.9))}",
          ".tfb-ball.tfb-active .tfb-orb-rim{background:conic-gradient(from 200deg, rgba(255,255,255,.6), rgba(255,200,110,.4), rgba(255,150,30,.45), rgba(255,255,255,.15), rgba(255,190,80,.4), rgba(255,255,255,.6));animation-duration:2.6s}",
          ".tfb-ball.tfb-done{color:#eafff4;background:radial-gradient(circle at 50% 60%, #5dffa8 0%, #22c070 20%, #12875c 45%, #073c26 70%, #02150c 100%);box-shadow:0 0 26px rgba(93,255,168,.6),0 0 5px rgba(180,255,215,.45),0 4px 18px rgba(0,0,0,.55),inset 0 1px 3px rgba(255,255,255,.45),inset 0 -8px 16px rgba(93,255,168,.18),inset 0 0 14px rgba(40,200,120,.3);text-shadow:0 0 4px rgba(255,255,255,1),0 0 12px rgba(190,255,220,1),0 0 24px rgba(60,255,150,.85),0 2px 4px rgba(0,40,20,.8);filter:drop-shadow(0 0 8px rgba(140,255,195,.95))}",
          ".tfb-ball.tfb-pending{color:#e8f3ff;background:radial-gradient(circle at 50% 60%, #7fc4ff 0%, #4f92e0 20%, #2f6cb0 45%, #12305c 70%, #04101e 100%);box-shadow:0 0 22px rgba(127,196,255,.5),0 0 4px rgba(180,220,255,.35),0 4px 18px rgba(0,0,0,.55),inset 0 1px 3px rgba(255,255,255,.4),inset 0 -8px 16px rgba(127,196,255,.15),inset 0 0 14px rgba(70,140,220,.25);text-shadow:0 0 4px rgba(255,255,255,.95),0 0 10px rgba(190,225,255,.9),0 0 20px rgba(90,170,255,.75),0 2px 4px rgba(0,20,45,.8);filter:drop-shadow(0 0 6px rgba(170,215,255,.85))}"
        ];
      } else if (th === "blue") {
        ball = [
          ".tfb-ball{position:fixed;right:18px;bottom:18px;z-index:2147483600;border-radius:50%;border:1px solid rgba(255,255,255,.28);cursor:grab;user-select:none;touch-action:none;pointer-events:auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;color:#a8c8ff;font-size:13px;font-weight:800;line-height:1.1;background:radial-gradient(circle at 30% 25%,rgba(255,255,255,.18),rgba(26,40,66,.96) 70%);box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.18),0 0 18px rgba(100,160,255,.5);transition:transform .15s ease}" + SIZE.replace(";", ";"),
          ".tfb-orb-band{display:none}",
          ".tfb-orb-rim{display:none}",
          ".tfb-ball .tfb-ring{position:absolute;inset:-1px;border-radius:50%;pointer-events:none;background:conic-gradient(var(--rc,#6aa8ff) calc(var(--pct,0)*1%),rgba(255,255,255,.08) 0);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 5px),#000 calc(100% - 4px));mask:radial-gradient(farthest-side,transparent calc(100% - 5px),#000 calc(100% - 4px));transition:background .4s ease;filter:drop-shadow(0 0 6px rgba(100,160,255,.5))}",
          ".tfb-ball .tfb-label{text-shadow:0 0 10px currentColor,0 0 3px currentColor}",
          ".tfb-ball.tfb-active{color:#ffcf8a;box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.18),0 0 16px rgba(255,170,51,.5);animation:tfb-pulse 1.6s ease-in-out infinite}",
          ".tfb-ball.tfb-active .tfb-ring{--rc:#ffaa33;filter:drop-shadow(0 0 8px rgba(255,170,51,.6))}",
          ".tfb-ball.tfb-done{color:#a5ffd4;box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.18),0 0 18px rgba(93,255,168,.5)}",
          ".tfb-ball.tfb-done .tfb-ring{--rc:#5dffa8;filter:drop-shadow(0 0 10px rgba(93,255,168,.7))}",
          ".tfb-ball.tfb-pending{color:#bcd9ff;box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.18),0 0 14px rgba(127,196,255,.5)}",
          ".tfb-ball.tfb-pending .tfb-ring{--rc:#7fc4ff;filter:drop-shadow(0 0 8px rgba(127,196,255,.6))}"
        ];
      } else if (th === "nebB") {
        ball = [
          ".tfb-ball{position:fixed;right:18px;bottom:18px;z-index:2147483600;border-radius:50%;border:1px solid rgba(255,255,255,.28);cursor:grab;user-select:none;touch-action:none;pointer-events:auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;color:#d9fff0;font-size:13px;font-weight:800;line-height:1.1;background:radial-gradient(circle at 50% 62%, rgba(210,255,244,.5) 0%, rgba(45,212,168,.75) 14%, rgba(18,160,130,.85) 30%, rgba(12,110,92,.95) 50%, rgba(6,40,34,1) 72%, rgba(2,16,14,1) 100%);box-shadow:0 0 20px rgba(45,212,168,.45),inset 0 2px 5px rgba(255,255,255,.45),inset 0 -10px 18px rgba(45,212,168,.25),inset 0 0 18px rgba(10,60,50,.5);text-shadow:0 0 6px rgba(120,255,214,.75),0 2px 4px rgba(0,40,30,.8);animation:tfbBreathe 3.2s ease-in-out infinite;transition:transform .15s ease}" + SIZE,
          ".tfb-orb-band{display:none}",
          ".tfb-orb-rim{position:absolute;inset:-1px;border-radius:50%;z-index:1;pointer-events:none;background:conic-gradient(from 200deg, rgba(255,255,255,.45), rgba(120,240,205,.3), rgba(10,200,170,.35), rgba(255,255,255,.12), rgba(45,212,168,.35), rgba(255,255,255,.45));-webkit-mask:radial-gradient(circle, transparent 63%, #000 66%, #000 72%, transparent 76%);mask:radial-gradient(circle, transparent 63%, #000 66%, #000 72%, transparent 76%);animation:tfbRimSpin 7s linear infinite;filter:blur(.5px)}",
          ".tfb-ball .tfb-ring{display:none}",
          ".tfb-ball .tfb-label{animation:tfbGlow 2.4s ease-in-out infinite}",
          ".tfb-ball.tfb-active{color:#ffd9a8;background:radial-gradient(circle at 50% 62%, rgba(255,240,210,.55) 0%, rgba(255,180,70,.8) 14%, rgba(220,130,25,.9) 30%, rgba(150,85,15,.95) 50%, rgba(60,32,6,1) 72%, rgba(24,12,2,1) 100%);box-shadow:0 0 20px rgba(255,170,51,.5),inset 0 2px 5px rgba(255,255,255,.5),inset 0 -10px 18px rgba(255,170,51,.28),inset 0 0 18px rgba(90,50,10,.5)}",
          ".tfb-ball.tfb-active .tfb-orb-rim{background:conic-gradient(from 200deg, rgba(255,255,255,.5), rgba(255,200,110,.35), rgba(255,150,30,.4), rgba(255,255,255,.12), rgba(255,190,80,.35), rgba(255,255,255,.5));animation-duration:2.6s}",
          ".tfb-ball.tfb-done{color:#d9fff0;background:radial-gradient(circle at 50% 62%, rgba(220,255,236,.5) 0%, rgba(93,255,168,.75) 14%, rgba(30,190,105,.85) 30%, rgba(15,130,75,.95) 50%, rgba(5,45,28,1) 72%, rgba(2,16,10,1) 100%);box-shadow:0 0 20px rgba(93,255,168,.5),inset 0 2px 5px rgba(255,255,255,.5),inset 0 -10px 18px rgba(93,255,168,.25),inset 0 0 18px rgba(8,60,38,.5)}",
          ".tfb-ball.tfb-pending{color:#dbeafe;background:radial-gradient(circle at 50% 62%, rgba(220,240,255,.5) 0%, rgba(127,196,255,.7) 14%, rgba(59,130,196,.85) 30%, rgba(35,90,150,.95) 50%, rgba(10,35,65,1) 72%, rgba(3,12,24,1) 100%);box-shadow:0 0 20px rgba(127,196,255,.45),inset 0 2px 5px rgba(255,255,255,.45),inset 0 -10px 18px rgba(127,196,255,.22),inset 0 0 18px rgba(15,45,85,.5)}"
        ];
      } else if (th === "nebC") {
        ball = [
          ".tfb-ball{position:fixed;right:18px;bottom:18px;z-index:2147483600;border-radius:50%;border:1px solid rgba(255,255,255,.28);cursor:grab;user-select:none;touch-action:none;pointer-events:auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;color:#d9fff0;font-size:13px;font-weight:800;line-height:1.1;background:radial-gradient(circle at 50% 55%, #1a8a70 0%, #0f6b58 30%, #083f35 60%, #031511 100%);box-shadow:0 0 18px rgba(45,212,168,.35),inset 0 2px 6px rgba(255,255,255,.28),inset 0 -8px 14px rgba(0,0,0,.5);text-shadow:0 0 6px rgba(120,255,214,.75),0 2px 4px rgba(0,40,30,.8);animation:tfbBreathe 3.2s ease-in-out infinite;transition:transform .15s ease}" + SIZE,
          ".tfb-ball::before{content:\"\";position:absolute;inset:4px 6px 58% 6px;border-radius:50%;background:radial-gradient(ellipse at 50% 30%, rgba(140,255,225,.55) 0%, rgba(140,255,225,.12) 60%, transparent 80%);filter:blur(1px);z-index:1;pointer-events:none}",
          ".tfb-orb-band{display:none}",
          ".tfb-orb-rim{position:absolute;inset:-1px;border-radius:50%;z-index:1;pointer-events:none;background:conic-gradient(from 200deg, rgba(255,255,255,.45), rgba(120,240,205,.3), rgba(10,200,170,.35), rgba(255,255,255,.12), rgba(45,212,168,.35), rgba(255,255,255,.45));-webkit-mask:radial-gradient(circle, transparent 63%, #000 66%, #000 72%, transparent 76%);mask:radial-gradient(circle, transparent 63%, #000 66%, #000 72%, transparent 76%);animation:tfbRimSpin 7s linear infinite;filter:blur(.5px)}",
          ".tfb-ball .tfb-ring{display:none}",
          ".tfb-ball .tfb-label{animation:tfbGlow 2.4s ease-in-out infinite}",
          ".tfb-ball.tfb-active{color:#ffd9a8;background:radial-gradient(circle at 50% 55%, #d98a1a 0%, #a86410 30%, #5c3608 60%, #201003 100%);box-shadow:0 0 18px rgba(255,170,51,.4),inset 0 2px 6px rgba(255,255,255,.3),inset 0 -8px 14px rgba(0,0,0,.5)}",
          ".tfb-ball.tfb-active::before{background:radial-gradient(ellipse at 50% 30%, rgba(255,220,160,.55) 0%, rgba(255,220,160,.12) 60%, transparent 80%)}",
          ".tfb-ball.tfb-active .tfb-orb-rim{background:conic-gradient(from 200deg, rgba(255,255,255,.5), rgba(255,200,110,.35), rgba(255,150,30,.4), rgba(255,255,255,.12), rgba(255,190,80,.35), rgba(255,255,255,.5));animation-duration:2.6s}",
          ".tfb-ball.tfb-done{color:#d9fff0;background:radial-gradient(circle at 50% 55%, #17a06a 0%, #0f7a4e 30%, #084a30 60%, #03150c 100%);box-shadow:0 0 18px rgba(93,255,168,.4),inset 0 2px 6px rgba(255,255,255,.28),inset 0 -8px 14px rgba(0,0,0,.5)}",
          ".tfb-ball.tfb-done::before{background:radial-gradient(ellipse at 50% 30%, rgba(180,255,220,.55) 0%, rgba(180,255,220,.12) 60%, transparent 80%)}",
          ".tfb-ball.tfb-pending{color:#dbeafe;background:radial-gradient(circle at 50% 55%, #2a6cb0 0%, #1c5288 30%, #10315c 60%, #04101e 100%);box-shadow:0 0 18px rgba(127,196,255,.35),inset 0 2px 6px rgba(255,255,255,.28),inset 0 -8px 14px rgba(0,0,0,.5)}",
          ".tfb-ball.tfb-pending::before{background:radial-gradient(ellipse at 50% 30%, rgba(180,215,255,.55) 0%, rgba(180,215,255,.12) 60%, transparent 80%)}"
        ];
      } else if (th === "glass") {
        ball = [
          ".tfb-ball{position:fixed;right:18px;bottom:18px;z-index:2147483600;border-radius:50%;border:1px solid rgba(255,255,255,.4);cursor:grab;user-select:none;touch-action:none;pointer-events:auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;color:#16324a;font-size:13px;font-weight:800;line-height:1.1;background:linear-gradient(145deg,rgba(190,225,245,.4) 0%,rgba(130,170,205,.28) 50%,rgba(85,115,150,.42) 100%);backdrop-filter:blur(4px);box-shadow:inset 0 1px 5px rgba(255,255,255,.55),inset 0 -8px 14px rgba(255,255,255,.1),0 4px 16px rgba(0,0,0,.35);transition:transform .15s ease}" + SIZE,
          ".tfb-orb-band{display:none}",
          ".tfb-orb-rim{display:none}",
          ".tfb-ball::before{content:\"\";position:absolute;inset:3px 3px 55% 3px;border-radius:50% 50% 40% 40%;background:linear-gradient(rgba(255,255,255,.55),rgba(255,255,255,.06));filter:blur(.5px);pointer-events:none}",
          ".tfb-ball .tfb-ring{position:absolute;inset:-1px;border-radius:50%;pointer-events:none;background:conic-gradient(var(--rc,#3b82c4) calc(var(--pct,0)*1%),rgba(255,255,255,.25) 0);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 4px),#000 calc(100% - 3px));mask:radial-gradient(farthest-side,transparent calc(100% - 4px),#000 calc(100% - 3px));transition:background .4s ease;filter:drop-shadow(0 0 5px rgba(80,140,200,.5))}",
          ".tfb-ball .tfb-label,.tfb-ball .tfb-sub{text-shadow:0 1px 2px rgba(255,255,255,.6)}",
          ".tfb-ball.tfb-active{color:#7a4a08;box-shadow:inset 0 1px 5px rgba(255,255,255,.55),inset 0 -8px 14px rgba(255,190,120,.15),0 4px 16px rgba(0,0,0,.35),0 0 14px rgba(255,170,51,.35)}",
          ".tfb-ball.tfb-active .tfb-ring{--rc:#ffaa33;filter:drop-shadow(0 0 7px rgba(255,170,51,.6))}",
          ".tfb-ball.tfb-done{color:#14532d;box-shadow:inset 0 1px 5px rgba(255,255,255,.55),inset 0 -8px 14px rgba(160,255,200,.15),0 4px 16px rgba(0,0,0,.35),0 0 14px rgba(93,255,168,.35)}",
          ".tfb-ball.tfb-done .tfb-ring{--rc:#16a34a;filter:drop-shadow(0 0 7px rgba(22,163,74,.6))}",
          ".tfb-ball.tfb-pending{color:#1e3a5f;box-shadow:inset 0 1px 5px rgba(255,255,255,.55),inset 0 -8px 14px rgba(170,205,255,.15),0 4px 16px rgba(0,0,0,.35),0 0 12px rgba(100,160,255,.3)}",
          ".tfb-ball.tfb-pending .tfb-ring{--rc:#3b82c4;filter:drop-shadow(0 0 6px rgba(59,130,196,.55))}"
        ];
      } else { // graphite
        ball = [
          ".tfb-ball{position:fixed;right:18px;bottom:18px;z-index:2147483600;border-radius:50%;border:1px solid rgba(255,255,255,.28);cursor:grab;user-select:none;touch-action:none;pointer-events:auto;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;color:#7defc4;font-size:13px;font-weight:800;line-height:1.1;background:radial-gradient(circle at 30% 25%,rgba(255,255,255,.15),rgba(40,44,54,.95) 70%);box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.15),0 0 14px rgba(45,212,168,.28);transition:transform .15s ease}" + SIZE.replace(";", ";"),
          ".tfb-orb-band{display:none}",
          ".tfb-orb-rim{display:none}",
          ".tfb-ball .tfb-ring{position:absolute;inset:-1px;border-radius:50%;pointer-events:none;background:conic-gradient(var(--rc,#2dd4a8) calc(var(--pct,0)*1%),rgba(255,255,255,.07) 0);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 5px),#000 calc(100% - 4px));mask:radial-gradient(farthest-side,transparent calc(100% - 5px),#000 calc(100% - 4px));transition:background .4s ease}",
          ".tfb-ball .tfb-label{text-shadow:0 0 8px currentColor,0 0 2px currentColor}",
          ".tfb-ball.tfb-active{color:#ffcf8a;box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.15),0 0 16px rgba(255,170,51,.45);animation:tfb-pulse 1.6s ease-in-out infinite}",
          ".tfb-ball.tfb-active .tfb-ring{--rc:#ffaa33}",
          ".tfb-ball.tfb-done{color:#8dffc8;box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.15),0 0 16px rgba(93,255,168,.4)}",
          ".tfb-ball.tfb-done .tfb-ring{--rc:#5dffa8}",
          ".tfb-ball.tfb-pending{color:#a8c8ec;box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.15),0 0 12px rgba(127,196,255,.3)}",
          ".tfb-ball.tfb-pending .tfb-ring{--rc:#7fc4ff}"
        ];
      }
      return [
        ":host{all:initial}",
        ".tfb-root{position:static;pointer-events:none;font-family:'Segoe UI','Microsoft YaHei',system-ui,sans-serif}",
        ".tfb-ball:hover{transform:scale(1.08)}",
        ".tfb-ball:active{cursor:grabbing;transform:scale(.95)}"
      ].concat(ball, [
        "@keyframes tfbBandFlow{0%,100%{transform:translateY(-3px) scaleY(.94);opacity:.8}50%{transform:translateY(3px) scaleY(1.12);opacity:1}}",
        "@keyframes tfbRimSpin{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}",
        "@keyframes tfbBreathe{0%,100%{transform:scale(.96);filter:brightness(.92)}50%{transform:scale(1.05);filter:brightness(1.12)}}",
        "@keyframes tfbGlow{0%,100%{text-shadow:0 0 4px rgba(255,255,255,.95),0 0 10px rgba(150,255,220,.9),0 0 20px rgba(30,220,180,.75)}50%{text-shadow:0 0 6px rgba(255,255,255,1),0 0 16px rgba(200,255,235,1),0 0 32px rgba(60,240,195,.95)}}",
        "@keyframes tfb-pulse{0%,100%{box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.15),0 0 16px rgba(255,170,51,.45),0 0 0 0 rgba(255,170,51,.5)}50%{box-shadow:0 4px 24px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,255,255,.15),0 0 16px rgba(255,170,51,.55),0 0 0 10px rgba(255,170,51,0)}}",
        ".tfb-ball.tfb-active .tfb-label{animation:none;text-shadow:0 0 4px rgba(255,255,255,.95),0 0 10px rgba(255,215,140,.95),0 0 20px rgba(255,160,40,.8)}",
        ".tfb-ball.tfb-done .tfb-label{animation:none;text-shadow:0 0 6px rgba(255,255,255,1),0 0 14px rgba(190,255,220,1),0 0 28px rgba(60,255,150,.9)}",
        // capsule mode (right-click toggle): long pill, wider, text-first
        ".tfb-ball.tfb-capsule{width:auto;min-width:220px;max-width:min(520px,60vw);height:44px;border-radius:22px;flex-direction:row;gap:8px;padding:0 16px;font-size:12px;justify-content:flex-start}",
        ".tfb-ball.tfb-capsule .tfb-orb-band{inset:1px}",
        ".tfb-ball.tfb-capsule .tfb-orb-rim{display:none}",
        ".tfb-ball.tfb-capsule .tfb-ring{display:none}",
        ".tfb-ball.tfb-capsule .tfb-label{flex-shrink:0}",
        ".tfb-ball.tfb-capsule .tfb-sub{flex:1;min-width:0;max-width:none;font-size:12px;text-align:left;overflow:hidden;white-space:nowrap}",
        ".tfb-ball.tfb-capsule .tfb-capsule-text{display:inline-block;white-space:nowrap}",
        ".tfb-ball.tfb-capsule .tfb-capsule-text.tfb-marquee{animation:tfbMarquee 9s ease-in-out infinite alternate}",
        "@keyframes tfbMarquee{from{transform:translateX(0)}to{transform:translateX(var(--shift,-40px))}}",
        ".tfb-panel{position:fixed;z-index:2147483600;width:300px;max-width:calc(100vw - 24px);max-height:74vh;overflow-y:auto;overflow-x:hidden;pointer-events:auto;display:none;color:#e6e6e6;font-size:13px;line-height:1.5;border-radius:14px;padding:12px 14px 10px;background:rgba(16,18,22,.92);border:1px solid rgba(255,255,255,.08);box-shadow:0 20px 60px rgba(0,0,0,.7),inset 0 1px 0 rgba(255,255,255,.06);backdrop-filter:blur(24px)}",
        ".tfb-panel.tfb-open{display:block}",
        ".tfb-panel.tfb-open.tfb-hz{display:flex;flex-wrap:wrap;align-items:flex-start;width:auto;min-width:540px;max-width:min(92vw,800px)}",
        ".tfb-panel.tfb-hz .tfb-head{flex:0 0 100%;width:100%}",
        ".tfb-colcap{display:none;font-size:11px;font-weight:600;color:#8fd3c0;padding:2px 0 6px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}",
        ".tfb-panel.tfb-hz .tfb-colcap{display:block}",
        ".tfb-panel.tfb-hz .tfb-col-cur{flex:1 1 45%;min-width:250px;max-width:none;box-sizing:border-box;padding-right:10px;border-right:1px solid rgba(255,255,255,.10)}",
        ".tfb-panel.tfb-hz .tfb-col-pin{flex:1 1 50%;min-width:260px;box-sizing:border-box;padding-left:10px}",
        ".tfb-head{display:flex;align-items:center;gap:6px;margin-bottom:6px;padding-bottom:6px;border-bottom:1px solid rgba(45,212,168,.25)}",
        ".tfb-title{font-weight:700;font-size:13px;white-space:nowrap}",
        ".tfb-ctitle{flex:1;min-width:0;font-size:11px;color:#9aa3b2;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;cursor:text}",
        ".tfb-summary{font-size:11px;color:#9aa3b2;white-space:nowrap}",
        ".tfb-btn{border:0;background:transparent;color:#9aa3b2;font-size:13px;cursor:pointer;padding:2px 5px;border-radius:6px;flex-shrink:0}",
        ".tfb-btn:hover{color:#e6e6e6;background:rgba(255,255,255,.06)}",
        ".tfb-btn.tfb-on{color:#ffd27a}",
        ".tfb-list{list-style:none;margin:0;padding:0}",
        ".tfb-item{display:flex;align-items:flex-start;gap:8px;padding:5px 4px;border-radius:8px;word-break:break-word}",
        ".tfb-item:nth-child(odd){background:rgba(255,255,255,.03)}",
        ".tfb-ico{flex-shrink:0;width:16px;height:16px;border-radius:50%;display:inline-flex;align-items:center;justify-content:center;font-size:10px;margin-top:1px}",
        ".tfb-item[data-status='completed'] .tfb-ico{background:rgba(74,222,128,.18);color:#5dffa8;border:1px solid rgba(93,255,168,.4)}",
        ".tfb-item[data-status='in_progress'] .tfb-ico{background:rgba(255,170,51,.2);color:#ffaa33;border:1px solid rgba(255,170,51,.45)}",
        ".tfb-item[data-status='pending'] .tfb-ico{background:rgba(127,196,255,.14);color:#7fc4ff;border:1px dashed rgba(127,196,255,.5)}",
        ".tfb-item[data-status='in_progress']{background:rgba(255,170,51,.06)}",
        ".tfb-item[data-status='in_progress'] .tfb-txt{color:#ffcf8a;font-weight:600}",
        ".tfb-item[data-status='completed'] .tfb-txt{color:#7fae94;text-decoration:line-through}",
        ".tfb-item[data-status='pending'] .tfb-txt{color:#aebfd4}",
        ".tfb-txt{flex:1}",
        // Plan-A2: per-row hide button — invisible until hover/focus, never
        // printed, keeps the row layout unchanged
        ".tfb-x{flex-shrink:0;border:0;background:transparent;color:#6b7280;font-size:11px;line-height:1;cursor:pointer;padding:2px 4px;border-radius:5px;opacity:0;transition:opacity .12s ease}",
        ".tfb-item:hover .tfb-x,.tfb-x:focus{opacity:1}",
        ".tfb-x:hover{color:#ff8a8a;background:rgba(255,120,120,.12)}",
        ".tfb-empty{color:#8b93a1;font-size:12px;text-align:center;padding:12px 0}",
        // Plan-A3: history fold toggle row
        ".tfb-histoggle{list-style:none;display:flex;align-items:center;justify-content:center;gap:4px;margin:6px 0 2px;padding:5px 4px;border-radius:8px;color:#9aa3b2;font-size:11px;cursor:pointer;background:rgba(255,255,255,.03)}",
        ".tfb-histoggle:hover{color:#e6e6e6;background:rgba(255,255,255,.06)}",
        ".tfb-sep{margin:10px 0 6px;padding-top:8px;border-top:1px solid rgba(255,255,255,.07);display:flex;align-items:center;justify-content:space-between}",
        ".tfb-sep-label{font-size:11px;color:#9aa3b2;font-weight:600}",
        ".tfb-pinrow{display:flex;align-items:center;gap:6px;padding:6px 4px;border-radius:8px;cursor:pointer}",
        ".tfb-pinrow:hover{background:rgba(255,255,255,.05)}",
        ".tfb-pinname{flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px}",
        ".tfb-pincount{font-size:11px;color:#9aa3b2;white-space:nowrap}",
        ".tfb-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0}",
        ".tfb-dot-active{background:#fbbf24;box-shadow:0 0 6px rgba(251,191,36,.8);animation:tfb-dotpulse 1.4s ease-in-out infinite}",
        ".tfb-dot-done{background:#4ade80}",
        ".tfb-dot-none{background:#6b7280}",
        "@keyframes tfb-dotpulse{0%,100%{opacity:1}50%{opacity:.4}}",
        ".tfb-pinlist{list-style:none;margin:2px 0 4px;padding:0 0 0 10px;border-left:2px solid rgba(255,255,255,.08)}",
        ".tfb-hint{font-size:10px;color:#6b7280;padding:6px 0 0;display:flex;align-items:center;justify-content:center;gap:4px;flex-wrap:wrap}",
        ".tfb-theme-menu{display:none;margin:-2px 0 8px;padding:6px;border:1px solid rgba(45,212,168,.25);border-radius:10px;background:rgba(12,16,20,.96)}",
        ".tfb-theme-menu.tfb-open{display:block}",
        ".tfb-theme-row{display:flex;align-items:center;gap:8px;padding:6px 8px;border-radius:8px;cursor:pointer;font-size:12px;color:#cdd6e2}",
        ".tfb-theme-row:hover{background:rgba(255,255,255,.06)}",
        ".tfb-theme-row.tfb-cur{color:#ffd27a;font-weight:600}",
        ".tfb-theme-row .tfb-theme-check{flex-shrink:0;width:14px;text-align:center}",
        ".tfb-theme-row .tfb-theme-label{flex:1;min-width:0}",
        ".tfb-rename{flex:1;min-width:0;padding:3px 8px;border-radius:6px;border:1px solid rgba(255,210,122,.5);background:rgba(0,0,0,.4);color:#ffd27a;font:inherit;font-size:12px;outline:none}",
        ".tfb-rename:focus{border-color:#ffd27a}",
        ".tfb-ctitle[contenteditable='true']{outline:none;border-bottom:1px dashed #ffd27a;color:#ffd27a;cursor:text}"
      ]).join("\n");
    }

    function listHtml(list) {
      var html = "";
      for (var i = 0; i < list.length; i++) {
        var t = list[i];
        var ico = t.status === "completed" ? "✓" : t.status === "in_progress" ? "▶" : "○";
        var txt = t.content.length > MAX_CONTENT ? t.content.slice(0, MAX_CONTENT) + "…" : t.content;
        // Plan-A2: every row carries a small ✕ so the user can hide history
        // rows they no longer want to see (only deletes from the DISPLAY —
        // the underlying session log is never touched; a new snapshot that
        // re-lists the item will show it again).
        // Plan-A6: a 📋 copy button next to it — copies the FULL task text
        // to the clipboard (handy for quoting a broken/odd task back).
        html += '<li class="tfb-item" data-status="' + t.status + '">' +
          '<span class="tfb-ico">' + ico + '</span>' +
          '<span class="tfb-txt">' + esc(txt) + '</span>' +
          '<button class="tfb-x" type="button" data-act="copyItem" data-copy="' + esc(t.content) + '" title="复制此条完整内容">📋</button>' +
          '<button class="tfb-x" type="button" data-act="hideItem" data-idx="' + i + '" title="从悬浮球隐藏此条（不动数据源）">✕</button>' +
          "</li>";
      }
      return html;
    }

    // Plan-A9: ONE body renderer shared by the current-session list and
    // every pinned-session list — snapshot rows, the two history folds
    // (per-sid open state), per-fold 🧹, the batch clear-all row and the
    // ♻️ restore-all row. `sid` is the list's OWN session: fold state,
    // hide scoping and every data-ownsid target key off it, so a pinned
    // session's controls can never act on the current session (or vice
    // versa). `list` is that session's RAW (unfiltered) merged list — the
    // hidden filter is applied per part, after the snapshot/history split.
    function historySectionHtml(sid, list) {
      var osid = normSid(sid);
      var raw = Array.isArray(list) ? list : [];
      var b = bySession[osid];
      var snapLen = (b && typeof b.snapLen === "number") ? Math.min(b.snapLen, raw.length) : raw.length;
      // Plan-A9: split the RAW list FIRST, then drop hidden rows from each
      // part — hiding a snapshot row must not drag a history row into the
      // snapshot area (nor shrink the folds).
      var snapRows = visibleList(osid, raw.slice(0, snapLen));
      var histRows = visibleList(osid, raw.slice(snapLen));
      var histDone = [];
      var histOpen_ = []; // Plan-A3.1: non-completed history gets its own fold
      for (var hi = 0; hi < histRows.length; hi++) {
        if (histRows[hi].status === "completed") histDone.push(histRows[hi]);
        else histOpen_.push(histRows[hi]);
      }
      // every action row carries data-ownsid so the dispatcher can scope
      // toggle/🧹/♻️ to THIS session (continuation of Plan-A2.2)
      var ownAttr = ' data-ownsid="' + esc(sid) + '"';
      var html = listHtml(snapRows);
      // Plan-A5: batch-clear affordance — one click hides every archive row
      // (both folds), same ✕ semantics as single-row hides, snapshot rows
      // and data layer untouched.
      var anyHist = histDone.length + histOpen_.length;
      if (histDone.length) {
        html += '<li class="tfb-histoggle" data-act="toggleHistory"' + ownAttr + ' title="展开/收起历史任务">▾ 已完成历史 ' + histDone.length + ' 条（点击' + (historyOpen[osid] ? "收起" : "展开") + '）' +
          '<button class="tfb-btn" data-act="clearHistory"' + ownAttr + ' type="button" title="一键隐藏本区全部条目（✕ 同款，仅显示层）">🧹</button></li>';
        if (historyOpen[osid]) html += listHtml(histDone);
      }
      // Plan-A3.1: non-completed history in a SECOND fold (default closed)
      if (histOpen_.length) {
        html += '<li class="tfb-histoggle" data-act="toggleHistoryOpen"' + ownAttr + ' title="展开/收起被抛弃的历史任务">▸ 被抛弃的历史任务 ' + histOpen_.length + ' 条（点击' + (historyOpenOpen[osid] ? "收起" : "展开") + '）' +
          '<button class="tfb-btn" data-act="clearHistoryOpen"' + ownAttr + ' type="button" title="一键隐藏本区全部条目（✕ 同款，仅显示层）">🧹</button></li>';
        if (historyOpenOpen[osid]) html += listHtml(histOpen_);
      }
      if (anyHist) {
        html += '<li class="tfb-histoggle" data-act="clearAll"' + ownAttr + ' title="一键隐藏两个历史区全部条目">🧹 一键清理全部历史（' + anyHist + ' 条）</li>';
      }
      // Plan-A7: restore affordance — one click un-hides EVERYTHING hidden
      // in this session (single ✕ hides and batch clears alike). Only
      // shown when at least one row is hidden, so it never adds noise.
      var hidCount = 0;
      var hidMap = hiddenFor(osid);
      for (var hk in hidMap) { if (hidMap[hk]) hidCount++; }
      if (hidCount > 0) {
        html += '<li class="tfb-histoggle" data-act="restoreAll"' + ownAttr + ' title="取消本会话全部隐藏（✕/🧹 的反向操作）">♻️ 一键恢复全部隐藏的历史（' + hidCount + ' 条）</li>';
      }
      return html;
    }

    // Plan-A9: which session does a clicked action row belong to? Rows
    // rendered by historySectionHtml carry data-ownsid directly; pinned
    // rows are additionally covered by their <ul data-ownsid>; everything
    // else targets the current session. The guards keep stub/DOM oddities
    // from throwing.
    function actionSid(t) {
      var own = null;
      try { own = t && t.getAttribute ? t.getAttribute("data-ownsid") : null; } catch (e) { own = null; }
      if (own) return own;
      // back-compat: the brief Plan-A8 pinned-fold rows carried data-sid
      try { own = t && t.getAttribute ? t.getAttribute("data-sid") : null; } catch (e1) { own = null; }
      if (own) return own;
      try {
        var li = t && t.closest ? t.closest(".tfb-item") : null;
        var ul = li && li.closest ? li.closest(".tfb-pinlist") : null;
        var own2 = ul && ul.getAttribute ? ul.getAttribute("data-ownsid") : null;
        if (own2) return own2;
      } catch (e2) {}
      return currentSid;
    }

    // Plan-A9 back-compat: the pinned-fold action names emitted by the
    // short-lived Plan-A8 attempt map onto the unified fold actions — ONE
    // implementation and ONE per-sid fold-state map for both list kinds.
    var ACT_ALIAS = {
      toggleHistPinned: "toggleHistory",
      toggleHistOpenPinned: "toggleHistoryOpen",
      clearHistPinned: "clearHistory",
      clearHistOpenPinned: "clearHistoryOpen"
    };

    function pinDotClass(sid) {
      var list = sessionTodos(sid);
      if (!list || !list.length) return "tfb-dot-none";
      var c = countsOf(list);
      if (c.done === c.total) return "tfb-dot-done";
      if (c.active > 0) return "tfb-dot-active";
      return "tfb-dot-none";
    }

    function renderList() {
      if (!ui.list) return;
      // Plan-A4: header summary reports the CURRENT SNAPSHOT only (history
      // folds are an archive and stay out of every counter).
      var c = snapshotList().length ? countsOf(snapshotList()) : { done: 0, active: 0, total: 0 };
      if (ui.ctitle) {
        // while the rename editor is live, don't rebuild the title cell
        if (editing.sid === normSid(currentSid)) { /* keep editor */ }
        else {
          ui.ctitle.textContent = currentSid ? sessionTitle(currentSid) : "";
          if (ui.capCur) ui.capCur.textContent = "▸ " + (ui.ctitle.textContent || "当前对话");
          ui.ctitle.title = currentSid ? "点击重命名此对话" : "";
        }
      }
      if (ui.summary) {
        var parts = [];
        if (c.done > 0) parts.push(c.done + " 完成");
        if (c.active > 0) parts.push(c.active + " 进行中");
        var pend = c.total - c.done - c.active;
        if (pend > 0) parts.push(pend + " 待办");
        ui.summary.textContent = parts.join(" · ");
      }
      if (ui.pinBtn) {
        var on = currentSid && isPinned(currentSid);
        ui.pinBtn.classList.toggle("tfb-on", !!on);
        ui.pinBtn.title = on ? "取消固定当前会话" : "固定当前会话（切换对话后仍监控）";
        ui.pinBtn.textContent = on ? "📍" : "📌";
      }
      var list = currentList();
      if (!list.length) {
        ui.list.innerHTML = '<li class="tfb-empty">当前会话暂无任务清单<br><span style="font-size:10px">AI 使用 todo_write 后自动显示；📌 可固定会话</span></li>';
      } else {
        // Plan-A9: the whole body (snapshot rows + the two history folds +
        // batch-clear + restore) comes from the ONE shared renderer, scoped
        // to the current session; renderPinned() reuses it per pinned sid.
        var html = historySectionHtml(currentSid, sessionTodos(currentSid));
        ui.list.innerHTML = html;
        // Plan-A4: counts from the CURRENT SNAPSHOT only. (User request
        // 2026-09-13: the dsh-client-auto-continue recommendation line is
        // removed — no third-party plugin advertising in this UI.)
        var cc = countsOf(snapshotRowsOf(currentSid));
        if (cc.done < cc.total) {
          var left = cc.total - cc.done;
          ui.list.innerHTML += '<li class="tfb-empty" style="padding:6px 0 2px;font-size:10px">⚠️ 还有 ' + left + ' 项未完成</li>';
        }
      }
      renderPinned();
    }

    function renderPinned() {
      if (!ui.pinSec) return;
      if (pinned.length === 0) { ui.pinSec.innerHTML = ""; return; }
      if (editing.sid) return; // a rename editor is live — don't rebuild under it
      var html = '<div class="tfb-sep"><span class="tfb-sep-label">📌 已固定 (' + pinned.length + ')</span></div>';
      for (var i = 0; i < pinned.length; i++) {
        var sid = pinned[i];
        var list = sessionTodosVisible(sid);
        // Plan-A9: the header count reports the LATEST SNAPSHOT only (same
        // A4 rule as the current list — the history folds are an archive
        // and stay out of every counter).
        var c = list ? countsOf(snapshotRowsOf(sid)) : null;
        var tname = sessionTitle(sid);
        var open = !!openPin[sid];
        html += '<div class="tfb-pinrow" data-act="togglePin" data-sid="' + esc(sid) + '">';
        html += '<span class="tfb-dot ' + pinDotClass(sid) + '"></span>';
        html += '<span class="tfb-pinname" title="' + esc(tname) + '">' + esc(tname) + "</span>";
        html += '<span class="tfb-pincount">' + (c ? c.done + "/" + c.total : "…") + "</span>";
        html += '<button class="tfb-btn" data-act="renamePin" data-sid="' + esc(sid) + '" title="重命名">✏️</button>';
        html += '<button class="tfb-btn" data-act="unpin" data-sid="' + esc(sid) + '" title="取消固定">✖</button>';
        html += "</div>";
        if (open) {
          if (list && list.length) {
            // Plan-A9: pinned sessions render through the SAME shared body
            // renderer as the current list — snapshot/history split, the two
            // folds (open state kept per sid), per-fold 🧹, batch clear-all
            // and ♻️ restore-all, every action scoped to THIS sid.
            html += '<ul class="tfb-pinlist" data-ownsid="' + esc(sid) + '">' + historySectionHtml(sid, sessionTodos(sid)) + "</ul>";
          } else {
            html += '<ul class="tfb-pinlist"><li class="tfb-empty" style="padding:6px 0">暂无数据（该会话产生 todo_write 后自动出现）</li></ul>';
          }
        }
      }
      ui.pinSec.innerHTML = html;
    }

    var THEME_NAMES = { nebula: "🌌 星云·流光", nebB: "🌟 星云·宝石", nebC: "🌙 星云·顶弧", graphite: "💎 石墨版", blue: "🔵 蓝宝石版", glass: "🧊 玻璃版" };

    function renderThemeMenu() {
      if (!ui.themeMenu) return;
      var html = "";
      function swdot(s) {
        var a = s.vars && s.vars.stA ? s.vars.stA : "#888";
        var b = s.vars && s.vars.bgB ? s.vars.bgB : "#222";
        return '<span class="tfb-swdot" style="background:linear-gradient(135deg,' + a + ',' + b + ')"></span>';
      }
      if (SK) {
        var groups = SK.groups();
        for (var g = 0; g < groups.length; g++) {
          html += '<div class="tfb-theme-cap">' + groups[g] + '</div>';
          for (var i = 0; i < SK.ALL.length; i++) {
            var sp = SK.ALL[i];
            if (sp.group !== groups[g]) continue;
            var cur = sp.id === currentSkinId;
            html += '<div class="tfb-theme-row' + (cur ? " tfb-cur" : "") + '" data-act="setTheme" data-sid="' + sp.id + '" title="' + sp.en + ' (方案 ' + sp.no + ')">' +
              '<span class="tfb-theme-check">' + (cur ? "✓" : " ") + "</span>" + swdot(sp) +
              '<span class="tfb-theme-label">' + sp.no + " · " + sp.name + "</span></div>";
          }
        }
        html += '<div class="tfb-theme-cap">经典（旧版）</div>';
        var order = ["nebula", "nebB", "nebC", "graphite", "blue", "glass"];
        for (var j = 0; j < order.length; j++) {
          var th = order[j];
          var cur2 = !currentSkinId && th === currentTheme;
          html += '<div class="tfb-theme-row' + (cur2 ? " tfb-cur" : "") + '" data-act="setTheme" data-sid="legacy:' + th + '">' +
            '<span class="tfb-theme-check">' + (cur2 ? "✓" : " ") + "</span>" +
            '<span class="tfb-theme-label">' + (THEME_NAMES[th] || th) + "</span></div>";
        }
      } else {
        var order2 = ["nebula", "nebB", "nebC", "graphite", "blue", "glass"];
        for (var k = 0; k < order2.length; k++) {
          var th2 = order2[k];
          var cur3 = th2 === currentTheme;
          html += '<div class="tfb-theme-row' + (cur3 ? " tfb-cur" : "") + '" data-act="setTheme" data-sid="' + th2 + '">' +
            '<span class="tfb-theme-check">' + (cur3 ? "✓" : " ") + "</span>" +
            '<span class="tfb-theme-label">' + (THEME_NAMES[th2] || th2) + "</span></div>";
        }
      }
      ui.themeMenu.innerHTML = html;
    }

    function renderBall() {
      if (!ui.ball) return;
      var st = ballState();
      ui.ball.className = "tfb-ball " + st.cls + (capsule ? " tfb-capsule" : "");
      var labelEl = ui.ball.querySelector(".tfb-label");
      if (labelEl) labelEl.textContent = st.label;
      // progress ring: --pct = done/total*100 (0 when no list; 100 when all
      // done). The conic-gradient ring visualizes completion at a glance.
      // Plan-A4: ring/capsule numbers report the CURRENT SNAPSHOT only.
      var pct = 0;
      var list = snapshotList();
      if (list.length) {
        var c0 = countsOf(list);
        pct = c0.total > 0 ? Math.round(c0.done / c0.total * 100) : 0;
      }
      try { ui.ball.style.setProperty("--pct", String(pct)); } catch (e) {}
      var sub = "";
      if (capsule) {
        // capsule mode: show the running task (or overall status) in full;
        // overflowing text scrolls back and forth (marquee)
        if (list.length) {
          var c1 = countsOf(list);
          if (c1.done === c1.total) sub = "✓ 全部完成 " + c1.done + "/" + c1.total;
          else {
            var running = "";
            for (var r = 0; r < list.length; r++) {
              if (list[r].status === "in_progress") { running = list[r].content; break; }
            }
            // v0.12.1: when nothing is in_progress, fall back to the first
            // PENDING item's text instead of showing bare numbers — users
            // reported the capsule showing only "待办 N/M" with no task text
            // whenever the list was completed + pending (no running item).
            var nextPend = "";
            for (var p = 0; p < list.length; p++) {
              if (list[p].status === "pending") { nextPend = list[p].content; break; }
            }
            sub = running
              ? "▶ " + running
              : nextPend
                ? "○ " + nextPend
                : "待办 " + c1.done + "/" + c1.total;
          }
        } else if (pinned.length) {
          for (var i = 0; i < pinned.length; i++) {
            var pl = sessionTodos(pinned[i]);
            if (pl && countsOf(pl).active > 0) { sub = "📌 " + (sessionTitle(pinned[i]) || "固定会话") + " 进行中"; break; }
          }
          if (!sub) sub = "📋 暂无进行中任务";
        } else {
          sub = "📋 暂无任务";
        }
      }
      // ROUND-BALL MODE: show ONLY the progress (user requirement) — no
      // task content on the ball face, since it never fits. The label is
      // done/total (or ✓); the sub line stays empty.
      var subEl = ui.ball.querySelector(".tfb-sub");
      if (subEl) {
        if (capsule) {
          subEl.innerHTML = '<span class="tfb-capsule-text">' + esc(sub) + "</span>";
          var textEl = subEl.querySelector(".tfb-capsule-text");
          if (textEl) {
            // measure AFTER layout: if the text overflows the pill, set the
            // scroll distance so the marquee ping-pongs exactly the overflow
            requestAnimationFrame(function () {
              try {
                var over = textEl.scrollWidth - subEl.clientWidth;
                if (over > 8) {
                  textEl.style.setProperty("--shift", "-" + (over + 6) + "px");
                  textEl.classList.add("tfb-marquee");
                } else {
                  textEl.classList.remove("tfb-marquee");
                }
              } catch (e2) {}
            });
          }
        } else {
          subEl.textContent = "";
        }
      }
      ui.ball.title = capsule
        ? "胶囊模式（右键切回圆球；左键仍可展开面板）"
        : (list.length
          ? "Todo 悬浮球 — 当前最新任务 " + summarize(list).head + "（" + pct + "% 完成；历史折叠不计数；左键展开/折叠 · 右键胶囊 · 可拖动）"
          : "Todo 悬浮球 — 当前会话暂无任务（左键展开 · 右键胶囊 · 可拖动）");
    }

    var layoutHz = false;
    try { layoutHz = localStorage.getItem("dsh-todo-float-ball-layout") === "1"; } catch (e) {}

    function render() {
      renderBall();
      renderList();
      if (expanded && ui.panel) positionPanel();
    }

    function positionPanel() {
      if (!ui.panel || !ui.ball) return;
      var r = ui.ball.getBoundingClientRect();
      var pw = ui.panel.offsetWidth || 300;
      var ph = ui.panel.offsetHeight || 320;
      var left = r.left + r.width / 2 - pw / 2;
      left = Math.max(8, Math.min(window.innerWidth - pw - 8, left));
      var top = r.top - ph - 12;
      if (top < 8) top = r.bottom + 12;
      ui.panel.style.left = left + "px";
      ui.panel.style.top = top + "px";
    }

    function show() {
      try { applyThemeNow(); } catch (e) {}
      expanded = true;
      if (ui.panel) { ui.panel.classList.add("tfb-open"); positionPanel(); }
    }
    function hide() {
      try { applyThemeNow(); } catch (e) {}
      expanded = false;
      if (ui.panel) ui.panel.classList.remove("tfb-open");
      // 字体插件同款：收起面板时刷新整个插件界面（重载 UI，主题/结构即时生效）
      setTimeout(function () { try { refreshPlugin(); } catch (e) {} }, 120);
    }
    function refreshPlugin() {
      try {
        var h = document.getElementById(BALL_ID);
        if (h) h.remove();
        ui = { root: null, ball: null, panel: null, list: null, summary: null, ctitle: null, pinBtn: null, pinSec: null, themeMenu: null };
        expanded = false;
        buildUI();
        applyBallVisibility();
      } catch (e) {}
    }

    function buildUI() {
      if (document.getElementById(BALL_ID)) return;
      if (!document.body) return;
      try { fetch("/dsh-todo-float-ball/client-alive").catch(function () {}); } catch (e) {}

      var host = document.createElement("div");
      host.id = BALL_ID;

      var shadow = host.attachShadow({ mode: "open" });
      var style = document.createElement("style");
      style.textContent = cssText();
      shadow.appendChild(style);

      var root = document.createElement("div");
      root.className = "tfb-root";
      root.innerHTML =
        '<button class="tfb-ball tfb-idle" type="button">' +
        '<span class="tfb-orb-band"></span>' +
        '<span class="tfb-orb-rim"></span>' +
        '<span class="tfb-ring"></span>' +
        '<span class="tfb-label">✓</span><span class="tfb-sub"></span>' +
        "</button>" +
        '<div class="tfb-panel">' +
        '<div class="tfb-head">' +
        '<span class="tfb-title">📋</span>' +
        '<span class="tfb-ctitle"></span>' +
        '<button class="tfb-btn" data-act="toggleLayout" title="切换面板布局：上下/左右" type="button">⇄</button>' +
        '<span class="tfb-summary"></span>' +
        '<button class="tfb-btn" data-act="pin" type="button">📌</button>' +
        '<button class="tfb-btn" data-act="themeMenu" type="button" title="切换皮肤">🎨</button>' +
        '<button class="tfb-btn" data-act="close" type="button" title="折叠">✕</button>' +
        "</div>" +
        '<div class="tfb-theme-menu"></div>' +
        '<div class="tfb-col tfb-col-cur">' +
        '<div class="tfb-colcap" data-cap="cur"></div>' +
        '<ul class="tfb-list"></ul>' +
        "</div>" +
        '<div class="tfb-col tfb-col-pin">' +
        '<div class="tfb-colcap" data-cap="pin">📌 固定会话</div>' +
        '<div class="tfb-pinsec"></div>' +
        "</div>" +
        '<div class="tfb-hint"><button class="tfb-btn" data-act="resetPos" type="button" title="一键归位右下角">🎯 归位</button> · 切换对话自动跟随 · 📌 固定 · 🎨 皮肤 · Esc 折叠</div>' +
        "</div>";
      shadow.appendChild(root);

      // Mount to <html>, not body — DSH gives body containers transform/filter
      // which break position:fixed children (font-enhancer verified fix).
      var mount = document.documentElement || document.body;
      if (!host.parentNode) mount.appendChild(host);

      ui.root = root;
      ui.ball = root.querySelector(".tfb-ball");
      ui.panel = root.querySelector(".tfb-panel");
      ui.list = root.querySelector(".tfb-list");
      ui.ctitle = root.querySelector(".tfb-ctitle");
      ui.summary = root.querySelector(".tfb-summary");
      ui.pinBtn = root.querySelector('[data-act="pin"]');
      ui.pinSec = root.querySelector(".tfb-pinsec");
      ui.capCur = root.querySelector('[data-cap="cur"]');
      ui.themeMenu = root.querySelector(".tfb-theme-menu");

      // restore position (clamped inside viewport)
      var bw = ui.ball.offsetWidth || 56, bh = ui.ball.offsetHeight || 56;
      try {
        var raw = localStorage.getItem(POS_KEY);
        if (raw) {
          var pos = JSON.parse(raw);
          if (pos && typeof pos.x === "number" && typeof pos.y === "number" &&
              pos.x >= 0 && pos.y >= 0 &&
              pos.x <= window.innerWidth - bw && pos.y <= window.innerHeight - bh) {
            ui.ball.style.left = pos.x + "px";
            ui.ball.style.top = pos.y + "px";
            ui.ball.style.right = "auto";
            ui.ball.style.bottom = "auto";
          }
        }
      } catch (e) {}

      // drag + click-to-toggle (5px threshold separates the two)
      var drag = { active: false, moved: false, sx: 0, sy: 0, ox: 0, oy: 0 };
      ui.ball.addEventListener("pointerdown", function (e) {
        drag.active = true; drag.moved = false;
        drag.sx = e.clientX; drag.sy = e.clientY;
        var r = ui.ball.getBoundingClientRect();
        drag.ox = r.left; drag.oy = r.top;
        try { ui.ball.setPointerCapture(e.pointerId); } catch (err) {}
        e.preventDefault();
      });
      ui.ball.addEventListener("pointermove", function (e) {
        if (!drag.active) return;
        var dx = e.clientX - drag.sx, dy = e.clientY - drag.sy;
        if (!drag.moved && Math.abs(dx) + Math.abs(dy) < 5) return;
        drag.moved = true;
        var nx = Math.max(0, Math.min(window.innerWidth - (ui.ball.offsetWidth || bw), drag.ox + dx));
        var ny = Math.max(0, Math.min(window.innerHeight - (ui.ball.offsetHeight || bh), drag.oy + dy));
        ui.ball.style.left = nx + "px";
        ui.ball.style.top = ny + "px";
        ui.ball.style.right = "auto";
        ui.ball.style.bottom = "auto";
        try { localStorage.setItem(POS_KEY, JSON.stringify({ x: Math.round(nx), y: Math.round(ny) })); } catch (err) {}
        if (expanded) positionPanel();
      });
      function endDrag(e) {
        if (!drag.active) return;
        drag.active = false;
        try { ui.ball.releasePointerCapture(e.pointerId); } catch (err) {}
        if (!drag.moved) { if (expanded) hide(); else show(); }
      }
      ui.ball.addEventListener("pointerup", endDrag);
      ui.ball.addEventListener("pointercancel", endDrag);

      // RIGHT-CLICK: toggle capsule mode (long pill showing the running
      // task); persists in localStorage.
      ui.ball.addEventListener("contextmenu", function (e) {
        e.preventDefault();
        capsule = !capsule;
        try { localStorage.setItem(CAPSULE_KEY, capsule ? "1" : "0"); } catch (err) {}
        render();
      });

      // delegated actions inside the shadow root
      root.addEventListener("click", function (e) {
        var t = e.target && e.target.closest ? e.target.closest("[data-act]") : null;
        if (!t) {
          // clicking the title text (not while editing) starts a rename
          if (e.target === ui.ctitle && currentSid && hasSessionsSvc) startRename(currentSid);
          return;
        }
        var act = t.getAttribute("data-act");
        var sid = t.getAttribute("data-sid");
        // Plan-A9 back-compat: normalize the legacy Plan-A8 fold action names
        // onto the unified ones before dispatch (see ACT_ALIAS).
        if (ACT_ALIAS[act]) act = ACT_ALIAS[act];
        if (act === "close") hide();
        else if (act === "pin") {
          if (!currentSid) return;
          if (isPinned(currentSid)) unpin(currentSid); else pin(currentSid);
        } else if (act === "resetPos") {
          try { localStorage.removeItem(POS_KEY); } catch (err) {}
          if (ui.ball) { ui.ball.style.left = "auto"; ui.ball.style.top = "auto"; ui.ball.style.right = "18px"; ui.ball.style.bottom = "18px"; }
        } else if (act === "toggleHistory") {
          // Plan-A9: expand/collapse the completed-history fold — PER-SID
          // (data-ownsid targets the row's own session; the current list
          // falls back to currentSid).
          var sidT1 = normSid(actionSid(t));
          historyOpen[sidT1] = !historyOpen[sidT1];
          render();
        } else if (act === "toggleHistoryOpen") {
          // Plan-A3.1/Plan-A9: non-completed fold, per-sid as well.
          var sidT2 = normSid(actionSid(t));
          historyOpenOpen[sidT2] = !historyOpenOpen[sidT2];
          render();
        } else if (act === "clearHistory" || act === "clearHistoryOpen" || act === "clearAll") {
          // Plan-A5: batch hide — mark every archive row of the targeted
          // fold(s) as hidden (same ✕ semantics: display-only, per-session).
          // Plan-A9: target = the row's own session, never the current one.
          var ns5 = normSid(actionSid(t));
          var b5 = bySession[ns5];
          var hid5 = hiddenFor(ns5);
          var snapLen5 = (b5 && typeof b5.snapLen === "number") ? Math.min(b5.snapLen, (b5.todos || []).length) : 0;
          var todos5 = (b5 && b5.todos) || [];
          var from = act === "clearHistory" ? snapLen5 : snapLen5; // both folds start after snapshot
          var to = act === "clearHistory" ? todos5.length : todos5.length;
          var changed5 = false;
          for (var ci = from; ci < to; ci++) {
            var it5 = todos5[ci];
            if (!it5 || !it5.content) continue;
            if (act === "clearHistory" && it5.status !== "completed") continue;
            if (act === "clearHistoryOpen" && it5.status === "completed") continue;
            if (!hid5[it5.content]) { hid5[it5.content] = true; changed5 = true; }
          }
          if (changed5) { saveHidden(); delete historyOpen[ns5]; delete historyOpenOpen[ns5]; }
          render();
        } else if (act === "restoreAll") {
          // Plan-A7: un-hide EVERYTHING hidden in this session — the inverse
          // of ✕ and 🧹 combined. Display-only, scoped to this session.
          // Plan-A9: session = the row's own sid (data-ownsid first).
          e.stopPropagation();
          var ns7 = normSid(actionSid(t));
          var hid7 = hiddenFor(ns7);
          var restored7 = false;
          for (var k7 in hid7) { delete hid7[k7]; restored7 = true; }
          if (restored7) { saveHidden(); historyOpen[ns7] = true; historyOpenOpen[ns7] = true; }
          render();
        } else if (act === "toggleLayout") {
          // Plan-A10: panel layout switch — stacked (up/down) <-> side-by-side (left/right).
          e.stopPropagation();
          layoutHz = !layoutHz;
          try { localStorage.setItem("dsh-todo-float-ball-layout", layoutHz ? "1" : "0"); } catch (err) {}
          if (ui.panel) ui.panel.classList.toggle("tfb-hz", !!layoutHz);
          if (expanded) positionPanel();
          render();
        } else if (act === "copyItem") {
          // Plan-A6: copy the row's full task text. The payload rides in the
          // data-copy attribute (the visible text may be truncated at
          // MAX_CONTENT). BUGFIX (user report: paste was empty): read
          // data-copy explicitly — the generic `sid` variable only carries
          // data-sid, which copy buttons don't have (it copied "").
          e.stopPropagation();
          var cpText = t.getAttribute("data-copy") || "";
          var copyOk = false;
          // execCommand path FIRST: it is synchronous, works inside the
          // Electron webview without clipboard-write permission, and its
          // success is actually observable. The async clipboard API in this
          // environment can reject silently (no await in a delegated
          // handler), which looked like success but pasted nothing.
          try {
            var ta = document.createElement("textarea");
            ta.value = cpText;
            ta.style.position = "fixed";
            ta.style.top = "0";
            ta.style.left = "0";
            ta.style.opacity = "0";
            document.body.appendChild(ta);
            ta.focus();
            ta.select();
            copyOk = document.execCommand("copy");
            document.body.removeChild(ta);
          } catch (err5) { copyOk = false; }
          if (!copyOk) {
            // fallback: async clipboard API (needs permission; may fail)
            try {
              if (navigator && navigator.clipboard && navigator.clipboard.writeText) {
                navigator.clipboard.writeText(cpText);
                copyOk = true;
              }
            } catch (err6) { copyOk = false; }
          }
          // visual ack reflects the real result: ✓ copied / ⚠ failed
          try {
            t.textContent = copyOk ? "✓" : "⚠";
            setTimeout(function () { try { t.textContent = "📋"; } catch (e7) {} }, 800);
          } catch (err7) {}
        } else if (act === "themeMenu") {
          // toggle the skin picker inside the panel
          if (ui.themeMenu) ui.themeMenu.classList.toggle("tfb-open");
          renderThemeMenu();
          e.stopPropagation();
        } else if (act === "setTheme" && sid) {
          // pick a skin: "legacy:<id>" = classic skin; otherwise shared 128-catalog id
          if (sid.indexOf("legacy:") === 0) {
            currentSkinId = null;
            legacyPicked = true;
            currentTheme = sid.slice(7);
            try { localStorage.setItem(THEME_KEY, currentTheme); } catch (err) {}
            try { localStorage.setItem(SHARED_KEY, "legacy"); } catch (err) {}
          } else if (SK && SK.BY_ID[sid]) {
            currentSkinId = sid;
            legacyPicked = false;
            SK.setSkin(sid);
          }
          try {
            var stEl = ui.root.querySelector("style");
            if (stEl) stEl.textContent = cssText();
          } catch (err) {}
          render();
          renderThemeMenu();
          e.stopPropagation();
        } else if (act === "togglePin" && sid) {
          openPin[sid] = !openPin[sid];
          render();
        } else if (act === "renamePin" && sid) {
          e.stopPropagation();
          startRename(sid);
        } else if (act === "unpin" && sid) {
          e.stopPropagation();
          unpin(sid);
        } else if (act === "hideItem") {
          // Plan-A2: row ✕ — hide this item from the display (display-only;
          // the session log and the stored record are untouched).
          // Plan-A2.2: resolve the row's OWN session (a pinned list row
          // belongs to its pinned sid, not to the current one) — walk up to
          // the enclosing .tfb-pinlist's parent pinrow when present.
          e.stopPropagation();
          var li = t.closest ? t.closest(".tfb-item") : null;
          var txtEl = li ? li.querySelector(".tfb-txt") : null;
          if (txtEl) {
            // Plan-A2.2: the row's own session. Pinned lists carry
            // data-ownsid on their <ul> (set in renderPinned); current-list
            // rows fall back to currentSid.
            var rowSid = currentSid;
            var pinUl = li.closest ? li.closest(".tfb-pinlist") : null;
            if (pinUl && pinUl.getAttribute) {
              var own = pinUl.getAttribute("data-ownsid");
              if (own) rowSid = own;
            }
            hideItem(rowSid, txtEl.textContent);
          }
        }
      });

      window.addEventListener("resize", function () { if (expanded) positionPanel(); });
      document.addEventListener("keydown", function (e) { if (e.key === "Escape" && expanded) hide(); });

      render();
    }

    // ---------- entry ----------
    var inject = ["sessions"];
    var currentSessions = null;

    // Skin buttons live in the sibling settings section (React-rendered);
    // React synthetic events / refs proved unreliable there, so the buttons
    // carry plain data attributes and we delegate at the DOCUMENT capture
    // phase — immune to any re-render, and capture fires before anything
    // can stopPropagation.
    function installThemeDelegation() {
      if (window.__dshtfbThemeDelegated) return;
      window.__dshtfbThemeDelegated = true;
      document.addEventListener("click", function (e) {
        var t = e.target && e.target.closest ? e.target.closest("[data-tfb-theme]") : null;
        if (!t) return;
        var theme = t.getAttribute("data-tfb-theme");
        var legacySet = { nebula: 1, graphite: 1, blue: 1, glass: 1, nebB: 1, nebC: 1 };
        if (legacySet[theme]) {
          currentSkinId = null;
          legacyPicked = true;
          currentTheme = theme;
          try { localStorage.setItem(THEME_KEY, theme); } catch (err) {}
          try { localStorage.setItem(SHARED_KEY, "legacy"); } catch (err) {}
          applyThemeNow();
          return;
        }
        if (SK && SK.BY_ID[theme]) { SK.setSkin(theme); return; }
        return;
      }, true);
    }

    // ---------- ball visibility (driven by the shared settings section) ----------
    // The show/hide toggle lives in the shared "悬浮球导航" settings section
    // provided by dsh-skill-browser (same pattern as the font ball). We only
    // read the shared localStorage key and apply it defensively.
    var BALL_VIS_KEY = "dsh-tfb-ball-visible";
    function ballVisible() {
      // default TRUE (always visible), seeded on first run so the shared
      // settings section can flip it and have the choice persist.
      return getCfg(BALL_VIS_KEY, DEFAULTS.ballVisible) === "1";
    }
    function applyBallVisibility() {
      var b = document.querySelector("#" + BALL_ID);
      if (!b) return;
      var show = ballVisible();
      // Uninstall-fallback (ecosystem safety): the show/hide toggle lives in
      // the shared "悬浮球导航" section provided by dsh-skill-browser. If
      // that plugin is GONE and the stored flag says hidden, the ball would
      // be stuck invisible with no way back — in that case ignore the flag
      // and show the ball again.
      if (!show) {
        var providerGone = !document.getElementById("dsh-skill-browser-ball") &&
          !document.querySelector('[data-dsb-slot]');
        if (providerGone) show = true;
      }
      b.style.setProperty("display", show ? "" : "none", "important");
    }
    function watchBallVisibility() {
      // Long-lived, low-frequency: the shared settings section writes the
      // theme/visibility keys and expects them picked up — so this watcher
      // must NOT expire (the old 20-min cap made skin switching die after
      // 20 minutes). The sibling plugin also calls window.__dshtfbApplyTheme
      // directly for instant effect; the poll is just a safety net.
      setInterval(function () {
        try { applyBallVisibility(); } catch (e) {}
        // 2s SAFETY NET (v0.9.1): if the sessions.list.subscribe() callback
        // ever stops firing (shell re-render, dropped projection frame, a
        // remount), the ball would otherwise sit on stale todos forever.
        // This pull is effectively free: syncFromSessions() funnels into
        // setSessionTodos(), whose signature check returns early on an
        // identical list, and it only re-renders when something moved.
        try { syncFromSessions(); } catch (e) {}
        try {
          var v = peekCfg(THEME_KEY, DEFAULTS.theme);
          if (isTheme(v) && v !== currentTheme && !currentSkinId) applyThemeNow();
        } catch (e) {}
        try {
          if (SK) {
            var skv = readSharedKey();
            if (skv && skv !== "legacy" && skv !== currentSkinId && SK.BY_ID[skv]) applyThemeNow();
            else if (skv === "legacy" && currentSkinId) applyThemeNow();
          }
        } catch (e2) {}
      }, 2000);
    }
    // Immediate theme application — exposed as window.__dshtfbApplyTheme so
    // the sibling settings section can invoke it synchronously on click.
    function applyThemeNow() {
      try {
        var raw = readSharedKey();
        if (raw === "legacy") { currentSkinId = null; }
        else if (raw && SK && SK.BY_ID[raw]) { currentSkinId = raw; legacyPicked = false; }
        else if ((!raw || (SK && raw === SK.DEFAULT_ID)) && SK && !legacyPicked) { currentSkinId = SK.DEFAULT_ID; }
      } catch (e) {}
      if (!currentSkinId) {
        var v = getCfg(THEME_KEY, DEFAULTS.theme);
        currentTheme = isTheme(v) ? v : DEFAULTS.theme;
      }
      try {
        if (ui.root) {
          var st = ui.root.querySelector("style");
          if (st) st.textContent = cssText();
        }
      } catch (e) {}
      render();
    }

    function apply(ctx) {
      // First-run seed: fill any ABSENT preference with its code default so a
      // fresh install is immediately usable and the values stay editable.
      // Existing stored values win — getCfg never overwrites.
      ensureDefaultCfg();
      loadPinned();
      loadHidden();
      loadTheme();
      try { capsule = getCfg(CAPSULE_KEY, DEFAULTS.capsule) === "1"; } catch (e) {}
      restorePersisted();
      if (ui.panel && layoutHz) ui.panel.classList.add("tfb-hz");
      // primary data channel: the official sessions service
      try {
        if (ctx && ctx.sessions && ctx.sessions.list && typeof ctx.sessions.list.getSnapshot === "function") {
          currentSessions = ctx.sessions;
          hasSessionsSvc = true;
          try { ctx.sessions.list.subscribe(syncFromSessions); } catch (e) {}
          syncFromSessions();
        }
      } catch (e) {}
      // global hook: lets the shared settings section switch the theme
      // synchronously on click (no polling delay).
      try {
        window.__dshtfbApplyTheme = applyThemeNow;
      } catch (e) {}
      // unified skin sync: hook registry + events (same-document custom event,
      // cross-context storage event) + first-run seed of the shared key.
      try {
        window.__dshBallSkinHooks = window.__dshBallSkinHooks || [];
        if (window.__dshBallSkinHooks.indexOf(applyThemeNow) < 0) window.__dshBallSkinHooks.push(applyThemeNow);
      } catch (e) {}
      try {
        window.addEventListener("dsh-ball-skin-change", function () { applyThemeNow(); });
      } catch (e) {}
      try {
        window.addEventListener("storage", function (ev) { if (ev && ev.key === SHARED_KEY) applyThemeNow(); });
      } catch (e) {}
      try {
        if (SK && readSharedKey() == null) localStorage.setItem(SHARED_KEY, SK.DEFAULT_ID);
      } catch (e) {}
      // document-level delegation for the sibling skin buttons (React
      // synthetic events / refs unreliable in that slot — plain
      // data-attribute buttons + capture-phase listener always fire)
      installThemeDelegation();
      // UI
      if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", buildUI);
      } else {
        buildUI();
      }
      applyBallVisibility();
      watchBallVisibility();
      // fallback channel only when the sessions service is unavailable
      if (!hasSessionsSvc) installPanelObserver();
      // watchdog keeps the ball alive across shell rebuilds
      var tries = 0;
      var t = setInterval(function () {
        tries++;
        try {
          if (!document.getElementById(BALL_ID)) buildUI();
          else renderBall();
          if (hasSessionsSvc) syncFromSessions();
        } catch (e) {}
        if (tries > 300) clearInterval(t);
      }, 2000);
    }

    exports.apply = apply;
    exports.inject = inject;
    return module.exports;
  }
});
